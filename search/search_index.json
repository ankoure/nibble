{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"nibble","text":"<p>nibble is a GTFS-RT to MBTA V3 SSE adapter. It sits between raw transit agency realtime feeds and downstream consumers, normalizing inconsistent feed behavior, maintaining vehicle state across polling gaps, and emitting a clean Server-Sent Events stream shaped like the MBTA V3 API.</p>"},{"location":"#the-problem-it-solves","title":"The problem it solves","text":"<p>Raw GTFS-RT feeds have a few chronic data quality issues:</p> <ul> <li>Silent vehicle drops \u2014 vehicles temporarily lose their <code>trip_id</code> when they arrive at a terminus, change trips, or when agency AVL systems glitch. Naive consumers drop and re-add the vehicle, causing UI flickering and lost tracking context.</li> <li>Agency feed quirks \u2014 some agencies (e.g. RIPTA) publish non-standard <code>trip_id</code> formats that don't match their own static GTFS. nibble normalizes these before the data reaches consumers.</li> <li>Stop gaps between polls \u2014 polling every 15 seconds means vehicles silently skip stops. nibble interpolates synthetic stop events using the static schedule to fill these gaps.</li> </ul> <p>nibble's output carries <code>provenance</code> and <code>confidence</code> tags on every vehicle event so consumers can weight or filter data by quality.</p>"},{"location":"#data-flow","title":"Data flow","text":"<pre><code>GTFS-RT protobuf\n       |\n   fetch_feed          (gtfs/realtime.py)  \u2014 async HTTP, graceful error handling\n       |\n   normalizer          (normalizer/)       \u2014 agency-specific feed quirk fixes\n       |\n   _parse_feed         (poller.py)         \u2014 protobuf \u2192 VehicleEvent snapshots\n       |\n   StateStore          (state.py)          \u2014 resolution ladder, confidence tagging\n       |\n   reconcile           (reconciler.py)     \u2014 diff prev/curr \u2192 SSE events\n       |\n   interpolate         (interpolator.py)   \u2014 fill stop gaps with synthetic events\n       |\n   Broadcaster         (server.py)         \u2014 pub/sub to all SSE clients\n       |\n  GET /vehicles                            \u2014 SSE stream (MBTA V3 format)\n</code></pre>"},{"location":"#quickstart","title":"Quickstart","text":""},{"location":"#docker","title":"Docker","text":"<pre><code>docker build -t nibble .\ndocker run \\\n  -e NIBBLE_GTFS_RT_URL=https://agency.example/gtfs-rt/VehiclePositions.pb \\\n  -e NIBBLE_GTFS_STATIC_URL=https://agency.example/gtfs/static.zip \\\n  -p 8080:8080 \\\n  nibble\n</code></pre>"},{"location":"#local-development","title":"Local development","text":"<pre><code>uv sync\nNIBBLE_GTFS_RT_URL=https://... NIBBLE_GTFS_STATIC_URL=https://... python -m nibble.server\n</code></pre>"},{"location":"#configuration","title":"Configuration","text":"<p>All settings are read from <code>NIBBLE_*</code> environment variables or a <code>.env</code> file in the working directory.</p> Variable Default Description <code>NIBBLE_GTFS_RT_URL</code> required GTFS-RT VehiclePositions protobuf endpoint URL <code>NIBBLE_GTFS_STATIC_URL</code> required Static GTFS ZIP download URL <code>NIBBLE_POLL_INTERVAL_SECONDS</code> <code>15</code> Seconds between feed polls <code>NIBBLE_STALE_VEHICLE_THRESHOLD_SECONDS</code> <code>90</code> Seconds a vehicle can go without a <code>trip_id</code> before being dropped <code>NIBBLE_NORMALIZER</code> <code>\"default\"</code> Feed normalizer to use: <code>\"default\"</code> (pass-through) or <code>\"ripta\"</code> <code>NIBBLE_MAX_INTERPOLATION_STOPS</code> <code>3</code> Maximum stop-sequence gap to interpolate across <code>NIBBLE_HOST</code> <code>\"0.0.0.0\"</code> Server bind host <code>NIBBLE_PORT</code> <code>8080</code> Server bind port"},{"location":"#endpoints","title":"Endpoints","text":"Endpoint Description <code>GET /vehicles</code> SSE stream of vehicle events in MBTA V3 format <code>GET /health</code> JSON health check with last poll time and connected client count <p>See API Reference for full API reference.</p>"},{"location":"#running-tests","title":"Running tests","text":"<pre><code>uv run pytest\n</code></pre>"},{"location":"#adding-a-normalizer","title":"Adding a normalizer","text":"<ol> <li>Create a new file under <code>nibble/normalizer/</code>, subclassing <code>BaseNormalizer</code>:</li> </ol> <pre><code>from nibble.normalizer.base import BaseNormalizer\n\nclass MyAgencyNormalizer(BaseNormalizer):\n    def normalize(self, feed, gtfs):\n        # mutate feed as needed\n        return feed\n</code></pre> <ol> <li>Register it in <code>poller._get_normalizer()</code>:</li> </ol> <pre><code>if name == \"myagency\":\n    from nibble.normalizer.myagency import MyAgencyNormalizer\n    return MyAgencyNormalizer()\n</code></pre> <ol> <li>Set <code>NIBBLE_NORMALIZER=myagency</code> in your environment.</li> </ol>"},{"location":"#project-structure","title":"Project structure","text":"<pre><code>nibble/\n\u251c\u2500\u2500 config.py          # Pydantic Settings (NIBBLE_* env vars)\n\u251c\u2500\u2500 models.py          # Core dataclasses: Position, VehicleEvent, SSEEvent, Trip, StopTime\n\u251c\u2500\u2500 state.py           # Vehicle state machine and resolution ladder\n\u251c\u2500\u2500 poller.py          # Async poll loop orchestrator\n\u251c\u2500\u2500 reconciler.py      # Diff engine: prev/curr snapshots \u2192 SSE events\n\u251c\u2500\u2500 interpolator.py    # Schedule-aware stop-gap interpolation\n\u251c\u2500\u2500 emitter.py         # MBTA V3 JSON:API serialization\n\u251c\u2500\u2500 server.py          # Starlette SSE server, Broadcaster, health endpoint\n\u251c\u2500\u2500 gtfs/\n\u2502   \u251c\u2500\u2500 static.py      # Static GTFS ZIP loader and index\n\u2502   \u2514\u2500\u2500 realtime.py    # Async GTFS-RT protobuf fetcher\n\u2514\u2500\u2500 normalizer/\n    \u251c\u2500\u2500 base.py        # Abstract BaseNormalizer interface\n    \u251c\u2500\u2500 default.py     # Pass-through normalizer\n    \u2514\u2500\u2500 ripta.py       # RIPTA-specific trip_id normalization\n</code></pre> <p>For a deep dive into design decisions and the data pipeline, see ARCHITECTURE.md.</p>"},{"location":"api/","title":"API Reference","text":"<p>nibble exposes two HTTP endpoints.</p>"},{"location":"api/#get-vehicles","title":"<code>GET /vehicles</code>","text":"<p>An SSE (Server-Sent Events) stream of vehicle state events in MBTA V3 JSON:API format.</p> <p>Content-Type: <code>text/event-stream</code></p>"},{"location":"api/#connection-behavior","title":"Connection behavior","text":"<p>On connection, the client immediately receives a <code>reset</code> event containing all currently known vehicles. Subsequent events arrive as vehicles change state. The connection remains open until the client disconnects.</p> <pre><code>event: reset\ndata: [&lt;vehicle&gt;, &lt;vehicle&gt;, ...]\n\nevent: update\ndata: [&lt;vehicle&gt;, ...]\n\nevent: remove\ndata: [{\"id\": \"vehicle-123\"}, ...]\n</code></pre>"},{"location":"api/#event-types","title":"Event types","text":"Event Meaning <code>reset</code> Full snapshot of all currently tracked vehicles. Sent once on connection. <code>update</code> One or more vehicles changed state (position, trip, stop). Also used for new vehicles. <code>remove</code> One or more vehicles have been dropped from the feed. The <code>data</code> array contains only <code>{\"id\": \"...\"}</code> objects."},{"location":"api/#vehicle-resource-shape","title":"Vehicle resource shape","text":"<p>Each item in a <code>reset</code> or <code>update</code> data array is a JSON:API vehicle resource:</p> <pre><code>{\n  \"id\": \"vehicle-123\",\n  \"type\": \"vehicle\",\n  \"attributes\": {\n    \"current_status\": \"IN_TRANSIT_TO\",\n    \"current_stop_sequence\": 14,\n    \"direction_id\": 0,\n    \"label\": \"1234\",\n    \"latitude\": 41.8255,\n    \"longitude\": -71.4128,\n    \"bearing\": 270.0,\n    \"speed\": null,\n    \"updated_at\": \"2024-06-01T12:00:00+00:00\",\n    \"occupancy_status\": null,\n    \"provenance\": \"observed\",\n    \"confidence\": \"confirmed\"\n  },\n  \"relationships\": {\n    \"trip\": {\"data\": {\"id\": \"trip-456\", \"type\": \"trip\"}},\n    \"route\": {\"data\": {\"id\": \"route-1\", \"type\": \"route\"}},\n    \"stop\": {\"data\": {\"id\": \"stop-789\", \"type\": \"stop\"}}\n  }\n}\n</code></pre>"},{"location":"api/#attributes","title":"Attributes","text":"Field Type Description <code>current_status</code> string <code>\"INCOMING_AT\"</code>, <code>\"STOPPED_AT\"</code>, or <code>\"IN_TRANSIT_TO\"</code> <code>current_stop_sequence</code> int | null Stop sequence number from the current trip <code>direction_id</code> int | null <code>0</code> or <code>1</code> per GTFS direction <code>label</code> string | null Human-readable vehicle label (e.g. bus number) <code>latitude</code> float WGS84 decimal degrees <code>longitude</code> float WGS84 decimal degrees <code>bearing</code> float | null Heading in degrees (0-359), clockwise from north <code>speed</code> float | null Speed in meters per second <code>updated_at</code> ISO 8601 string Timestamp of this event <code>occupancy_status</code> null Always null; reserved for future use <code>provenance</code> string How this event was produced (see below) <code>confidence</code> string How certain nibble is about the trip assignment (see below)"},{"location":"api/#relationships","title":"Relationships","text":"<p>Each relationship's <code>data</code> is either a <code>{\"id\": \"...\", \"type\": \"...\"}</code> object or <code>null</code> if unknown.</p> Relationship Type Description <code>trip</code> <code>\"trip\"</code> The GTFS trip the vehicle is currently on <code>route</code> <code>\"route\"</code> The route the vehicle is serving <code>stop</code> <code>\"stop\"</code> The stop the vehicle is approaching or stopped at"},{"location":"api/#provenance-values","title":"<code>provenance</code> values","text":"<p><code>provenance</code> describes how the event's data was produced:</p> Value Meaning <code>\"observed\"</code> Directly reported by the GTFS-RT feed <code>\"inferred\"</code> Position was observed, but trip/route/stop was carried forward from a prior poll (vehicle temporarily lost its <code>trip_id</code>) <code>\"interpolated\"</code> Synthetic event generated to fill a stop gap between two observed polls <code>\"manual\"</code> Trip assignment issued by an operator via the corrections API <p>Consumers may display <code>\"interpolated\"</code> events differently (e.g. reduced opacity, dashed track line) to reflect that the position is nibble's estimate rather than a direct observation.</p>"},{"location":"api/#confidence-values","title":"<code>confidence</code> values","text":"<p><code>confidence</code> describes how certain nibble is about the vehicle's current trip assignment:</p> Value Meaning <code>\"confirmed\"</code> Vehicle reported a <code>trip_id</code> in this poll <code>\"inferred\"</code> Vehicle's <code>trip_id</code> is carried forward from a prior poll; it may have changed trips <code>\"stale\"</code> Vehicle exceeded the stale threshold; a <code>remove</code> event follows in the same cycle <p>In practice, <code>\"stale\"</code> events are immediately followed by a <code>remove</code> and should not reach display logic. Consumers may wish to treat <code>\"inferred\"</code> trips with reduced confidence when making routing or ETA calculations.</p>"},{"location":"api/#example-connecting-with-javascript","title":"Example: connecting with JavaScript","text":"<pre><code>const source = new EventSource(\"http://localhost:8080/vehicles\");\n\nsource.addEventListener(\"reset\", (e) =&gt; {\n  const vehicles = JSON.parse(e.data);\n  // replace all vehicles in your store\n});\n\nsource.addEventListener(\"update\", (e) =&gt; {\n  const vehicles = JSON.parse(e.data);\n  // upsert each vehicle\n});\n\nsource.addEventListener(\"remove\", (e) =&gt; {\n  const removed = JSON.parse(e.data);\n  // remove each vehicle by id\n});\n</code></pre>"},{"location":"api/#get-health","title":"<code>GET /health</code>","text":"<p>A JSON health check endpoint for use with load balancers and uptime monitors.</p> <p>Content-Type: <code>application/json</code></p>"},{"location":"api/#response","title":"Response","text":"<pre><code>{\n  \"status\": \"ok\",\n  \"last_poll_time\": \"2024-06-01T12:00:05+00:00\",\n  \"connected_clients\": 3\n}\n</code></pre> Field Type Description <code>status</code> string Always <code>\"ok\"</code> while the process is running <code>last_poll_time</code> ISO 8601 string | null Timestamp of the last successful feed poll; <code>null</code> if no poll has completed yet <code>connected_clients</code> int Number of currently connected SSE clients <p><code>last_poll_time</code> is only updated when the feed was fetched and events were broadcast. A stale <code>last_poll_time</code> (or <code>null</code>) indicates that the feed may be unreachable or returning errors. Check application logs for details.</p>"},{"location":"api/#post-trip_assignments","title":"<code>POST /trip_assignments</code>","text":"<p>Create or replace a manual trip assignment for a vehicle. See the manual trip assignment corrections guide for full details.</p> <p>Content-Type: <code>application/json</code></p>"},{"location":"api/#request-body","title":"Request body","text":"<pre><code>{ \"vehicle_id\": \"BUS-42\", \"trip_id\": \"trip-123\" }\n</code></pre>"},{"location":"api/#response-200-ok","title":"Response <code>200 OK</code>","text":"<pre><code>{ \"vehicle_id\": \"BUS-42\", \"trip_id\": \"trip-123\", \"assigned_at\": \"2024-06-01T12:34:56.789012+00:00\" }\n</code></pre> <p>Returns <code>422</code> if either field is missing or <code>trip_id</code> is not in the loaded static GTFS.</p>"},{"location":"api/#get-trip_assignments","title":"<code>GET /trip_assignments</code>","text":"<p>List all currently active manual trip overrides.</p> <p>Content-Type: <code>application/json</code></p>"},{"location":"api/#response-200-ok_1","title":"Response <code>200 OK</code>","text":"<pre><code>{\n  \"BUS-42\": { \"trip_id\": \"trip-123\", \"assigned_at\": \"2024-06-01T12:34:56.789012+00:00\" }\n}\n</code></pre>"},{"location":"api/#delete-trip_assignmentsvehicle_id","title":"<code>DELETE /trip_assignments/{vehicle_id}</code>","text":"<p>Remove the manual override for a vehicle. No-op if none exists.</p>"},{"location":"api/#response_1","title":"Response","text":"<p><code>204 No Content</code></p>"},{"location":"how-to/arrival-predictions/","title":"Arrival predictions","text":"<p>Nibble can compute a vehicle's current delay and predict its arrival times at remaining stops using current-delay propagation: the delay observed at the vehicle's current stop is applied uniformly to every remaining stop in the trip.</p> <p>This is a practical estimate suitable for passenger-facing displays and operational dashboards. It is not a schedule-adherence score \u2014 use it where you need \"when will this bus arrive?\" not \"how reliably does this route run?\"</p>"},{"location":"how-to/arrival-predictions/#how-it-works","title":"How it works","text":"<ol> <li>Nibble looks up the vehicle's scheduled departure time at its current stop in the static GTFS.</li> <li>It compares that to the vehicle's observed timestamp to compute a signed delay (positive = late, negative = early).</li> <li>That delay is added to the scheduled arrival time at every remaining stop in the trip.</li> </ol> <p>The approach is intentionally simple. It assumes the vehicle continues running at the same offset from schedule \u2014 no traffic prediction, no dwell-time modelling.</p>"},{"location":"how-to/arrival-predictions/#compute_delay","title":"<code>compute_delay</code>","text":"<p>Returns the vehicle's current delay in integer seconds, or <code>None</code> if it cannot be determined.</p> <pre><code>from nibble.predictions import compute_delay\n\ndelay = compute_delay(event, gtfs, agency_timezone=\"America/New_York\")\n# e.g. 120  \u2192 2 minutes late\n# e.g. -30  \u2192 30 seconds early\n# e.g. None \u2192 trip_id or stop_sequence missing, or stop not in schedule\n</code></pre> <p>Returns <code>None</code> when:</p> <ul> <li><code>event.trip_id</code> or <code>event.current_stop_sequence</code> is <code>None</code></li> <li>The trip has no stop-time data in the loaded static GTFS</li> <li>The current stop sequence is not found in the trip's stop times</li> <li>The scheduled time for that stop is missing</li> </ul>"},{"location":"how-to/arrival-predictions/#predict_arrivals","title":"<code>predict_arrivals</code>","text":"<p>Returns a list of predicted arrivals at all remaining stops (including the current stop), or <code>[]</code> if predictions cannot be computed.</p> <pre><code>from nibble.predictions import predict_arrivals\n\narrivals = predict_arrivals(event, gtfs, agency_timezone=\"America/New_York\")\n</code></pre> <p>Each entry in the list is a dict:</p> Field Type Description <code>stop_id</code> string GTFS stop identifier <code>stop_sequence</code> int Stop sequence number in the trip <code>scheduled_arrival</code> string ISO-8601 scheduled arrival time <code>predicted_arrival</code> string ISO-8601 predicted arrival time <code>delay_seconds</code> int Signed delay applied (positive = late) <p>Returns <code>[]</code> when:</p> <ul> <li><code>event.trip_id</code> or <code>event.current_stop_sequence</code> is <code>None</code></li> <li>The trip has no stop-time data in the loaded static GTFS</li> <li><code>compute_delay</code> returns <code>None</code></li> </ul>"},{"location":"how-to/arrival-predictions/#timezone-handling","title":"Timezone handling","text":"<p>Both functions accept an optional <code>agency_timezone</code> argument \u2014 an IANA timezone name such as <code>\"America/New_York\"</code> or <code>\"America/Los_Angeles\"</code>. This is used to convert the vehicle's UTC timestamp to local time-of-day for comparison against the GTFS schedule, which is expressed in local time.</p> <p>If <code>agency_timezone</code> is <code>None</code> or unrecognised, both functions fall back to UTC. For agencies in non-UTC timezones this will produce incorrect delay values, so always pass the correct timezone in production.</p> <p>The <code>NIBBLE_AGENCY_TIMEZONE</code> environment variable controls this for the built-in pipeline.</p>"},{"location":"how-to/arrival-predictions/#caveats","title":"Caveats","text":"<p>Predictions inherit data quality from the vehicle state. If a vehicle's position was interpolated or inferred, its timestamp may be stale and the computed delay will reflect that staleness. Filter by <code>provenance</code> before surfacing predictions to end users:</p> <ul> <li><code>\"observed\"</code> \u2014 delay is based on a fresh feed timestamp; most reliable</li> <li><code>\"inferred\"</code> \u2014 trip carried forward from a prior poll; delay may be minutes out of date</li> <li><code>\"interpolated\"</code> \u2014 synthetic stop event; timestamp is estimated, not observed</li> <li><code>\"manual\"</code> \u2014 operator-assigned trip; timestamp is still real, delay is usable</li> </ul> <p>Delay is not propagated across trip boundaries. If a vehicle is on the last stop of a trip, <code>predict_arrivals</code> returns only that stop.</p>"},{"location":"how-to/arrival-predictions/#example","title":"Example","text":"<pre><code>import asyncio\nimport httpx\nfrom nibble.predictions import compute_delay, predict_arrivals\n\n# Assume `gtfs` is a loaded StaticGTFS and `snapshot` is the current vehicle state.\n# In practice you'd obtain these from the nibble state store or a poller callback.\n\nAGENCY_TZ = \"America/New_York\"\n\nfor vehicle_id, event in snapshot.items():\n    if event.provenance not in (\"observed\", \"manual\"):\n        continue  # skip stale or synthetic data\n\n    delay = compute_delay(event, gtfs, AGENCY_TZ)\n    if delay is None:\n        continue\n\n    print(f\"{vehicle_id}: {delay:+d}s\")\n\n    arrivals = predict_arrivals(event, gtfs, AGENCY_TZ)\n    for stop in arrivals[:3]:  # next 3 stops\n        print(\n            f\"  stop {stop['stop_id']} (seq {stop['stop_sequence']}): \"\n            f\"predicted {stop['predicted_arrival']}  \"\n            f\"(sched {stop['scheduled_arrival']})\"\n        )\n</code></pre>"},{"location":"how-to/monitoring-headways/","title":"Monitoring headways","text":"<p>A headway is the time or distance gap between consecutive vehicles on a route. Monitoring headways lets you detect two common service quality problems:</p> <ul> <li>Bunching \u2014 two vehicles running very close together, leaving a large gap behind them</li> <li>Gapping \u2014 an unusually large hole in service, often because a vehicle has dropped off the route</li> </ul> <p>Nibble computes headways from the current resolved vehicle snapshot using static GTFS shape distances and scheduled departure times.</p>"},{"location":"how-to/monitoring-headways/#how-it-works","title":"How it works","text":"<p><code>compute_headways</code> filters the snapshot to vehicles on the requested route that have a resolved <code>trip_id</code>. It groups them by <code>direction_id</code>, then sorts each group furthest-ahead first using <code>shape_dist_traveled</code> at the vehicle's current stop. If <code>shape_dist_traveled</code> is not present in the agency's GTFS, it falls back to stop sequence.</p> <p>Consecutive pairs in each sorted group are annotated with two gap metrics:</p> <ul> <li><code>gap_to_previous_meters</code> \u2014 physical distance along the route shape between the two vehicles' current stops</li> <li><code>scheduled_gap_to_previous_seconds</code> \u2014 difference in scheduled departure times at each vehicle's current stop</li> </ul> <p>The lead vehicle in each direction (furthest ahead) has <code>null</code> for both gap fields.</p>"},{"location":"how-to/monitoring-headways/#compute_headways","title":"<code>compute_headways</code>","text":"<pre><code>from nibble.headways import compute_headways\n\nresult = compute_headways(route_id=\"39\", snapshot=snapshot, gtfs=gtfs)\n</code></pre> <p>Arguments:</p> Argument Type Description <code>route_id</code> string The GTFS route to compute headways for <code>snapshot</code> dict Current vehicle snapshot keyed by <code>vehicle_id</code> <code>gtfs</code> StaticGTFS Loaded static GTFS indexes <p>Return shape:</p> <pre><code>{\n  \"route_id\": \"39\",\n  \"directions\": [\n    {\n      \"direction_id\": 0,\n      \"vehicles\": [\n        {\n          \"vehicle_id\": \"y1234\",\n          \"trip_id\": \"trip-A\",\n          \"stop_id\": \"stop-10\",\n          \"stop_sequence\": 18,\n          \"shape_dist_traveled\": 4821.3,\n          \"scheduled_departure\": \"14:32:00\",\n          \"gap_to_previous_meters\": null,\n          \"scheduled_gap_to_previous_seconds\": null\n        },\n        {\n          \"vehicle_id\": \"y5678\",\n          \"trip_id\": \"trip-B\",\n          \"stop_id\": \"stop-7\",\n          \"stop_sequence\": 12,\n          \"shape_dist_traveled\": 2904.1,\n          \"scheduled_departure\": \"14:20:00\",\n          \"gap_to_previous_meters\": 1917.2,\n          \"scheduled_gap_to_previous_seconds\": 720\n        }\n      ]\n    }\n  ]\n}\n</code></pre> <p>Per-vehicle fields:</p> Field Type Description <code>vehicle_id</code> string Vehicle identifier <code>trip_id</code> string Resolved trip identifier <code>stop_id</code> string or null Current stop <code>stop_sequence</code> int or null Current stop sequence <code>shape_dist_traveled</code> float or null Distance along the shape to the current stop (from GTFS) <code>scheduled_departure</code> string or null Scheduled departure time at the current stop (HH:MM:SS) <code>gap_to_previous_meters</code> float or null Physical distance to the vehicle ahead; <code>null</code> for the lead vehicle or if <code>shape_dist_traveled</code> is missing <code>scheduled_gap_to_previous_seconds</code> int or null Scheduled time gap to the vehicle ahead; <code>null</code> for the lead vehicle or if schedule data is missing"},{"location":"how-to/monitoring-headways/#interpreting-the-results","title":"Interpreting the results","text":"<p><code>gap_to_previous_meters</code> measures where vehicles physically are on the route. A small value means two vehicles are close together.</p> <p><code>scheduled_gap_to_previous_seconds</code> measures how far apart the vehicles should be according to the schedule, based on when their current stops are timed. This is the more operationally useful metric:</p> <ul> <li>Positive \u2014 the vehicle ahead is scheduled later than the one behind (normal, expected spacing)</li> <li>Near zero or negative \u2014 the two vehicles are scheduled at almost the same time; this is bunching relative to the schedule regardless of physical distance</li> </ul> <p>A common pattern is to flag any pair where <code>scheduled_gap_to_previous_seconds</code> is below a threshold (e.g. 120 seconds) as bunched.</p> <p>Missing values: <code>gap_to_previous_meters</code> is <code>null</code> when <code>shape_dist_traveled</code> is absent from the agency's GTFS. <code>scheduled_gap_to_previous_seconds</code> is <code>null</code> when scheduled times are missing. Check your GTFS export if you see consistent nulls.</p>"},{"location":"how-to/monitoring-headways/#example","title":"Example","text":"<pre><code>from nibble.headways import compute_headways\n\nBUNCHING_THRESHOLD_SECONDS = 120  # flag pairs closer than 2 minutes scheduled\n\nresult = compute_headways(route_id=\"39\", snapshot=snapshot, gtfs=gtfs)\n\nfor direction in result[\"directions\"]:\n    print(f\"Direction {direction['direction_id']}\")\n    for vehicle in direction[\"vehicles\"]:\n        gap = vehicle[\"scheduled_gap_to_previous_seconds\"]\n        if gap is None:\n            continue\n        status = \"BUNCHED\" if gap &lt; BUNCHING_THRESHOLD_SECONDS else \"ok\"\n        print(\n            f\"  {vehicle['vehicle_id']} (trip {vehicle['trip_id']}): \"\n            f\"scheduled gap {gap}s [{status}]\"\n        )\n</code></pre> <p>Example output:</p> <pre><code>Direction 0\n  y1234 (trip trip-A): lead vehicle\n  y5678 (trip trip-B): scheduled gap 720s [ok]\n  y9012 (trip trip-C): scheduled gap 85s [BUNCHED]\n</code></pre>"},{"location":"how-to/publishing-gtfs-rt-to-s3/","title":"Publishing GTFS-RT feeds to S3","text":"<p>After each poll, nibble can re-publish its resolved vehicle state as standard GTFS-RT protobuf feeds to an S3 bucket. This lets downstream systems \u2014 other agencies, analytics pipelines, archival tools \u2014 consume GTFS-RT without connecting to nibble's SSE stream.</p> <p>Two feeds are available:</p> <ul> <li>VehiclePositions \u2014 every vehicle in the snapshot, with position, trip, and stop status. All vehicles are included regardless of whether a <code>trip_id</code> could be resolved.</li> <li>TripUpdates \u2014 vehicles with a resolved <code>trip_id</code> and known stop sequence, with a <code>StopTimeUpdate</code> for every remaining stop carrying the vehicle's current delay propagated forward. Vehicles without a <code>trip_id</code> or <code>current_stop_sequence</code> are omitted.</li> </ul>"},{"location":"how-to/publishing-gtfs-rt-to-s3/#prerequisites","title":"Prerequisites","text":"<p>Install the S3 extra:</p> <pre><code>pip install nibble[s3]\n</code></pre> <p>This adds <code>boto3</code> as a dependency. Boto3 picks up AWS credentials from the standard chain: environment variables (<code>AWS_ACCESS_KEY_ID</code> / <code>AWS_SECRET_ACCESS_KEY</code>), <code>~/.aws/credentials</code>, or an EC2/ECS instance profile.</p> <p>The IAM principal used must have <code>s3:PutObject</code> on the target keys:</p> <pre><code>{\n  \"Effect\": \"Allow\",\n  \"Action\": \"s3:PutObject\",\n  \"Resource\": [\n    \"arn:aws:s3:::my-bucket/gtfs-rt/vehicle_positions.pb\",\n    \"arn:aws:s3:::my-bucket/gtfs-rt/trip_updates.pb\"\n  ]\n}\n</code></pre>"},{"location":"how-to/publishing-gtfs-rt-to-s3/#publishing-vehiclepositions","title":"Publishing VehiclePositions","text":"<pre><code>import asyncio\nfrom nibble.vehicle_positions_publisher import publish_vehicle_positions\n\nawait publish_vehicle_positions(\n    snapshot=snapshot,\n    bucket=\"my-bucket\",\n    key=\"gtfs-rt/vehicle_positions.pb\",\n    region=\"us-east-1\",   # optional, defaults to us-east-1\n)\n</code></pre> <p>Every vehicle in <code>snapshot</code> becomes a <code>VehiclePosition</code> entity. The internal <code>current_status</code> string is mapped to the GTFS-RT enum:</p> nibble value GTFS-RT enum value <code>\"INCOMING_AT\"</code> <code>0</code> <code>\"STOPPED_AT\"</code> <code>1</code> <code>\"IN_TRANSIT_TO\"</code> <code>2</code> (default) <p>The feed is uploaded with <code>Content-Type: application/x-protobuf</code>.</p>"},{"location":"how-to/publishing-gtfs-rt-to-s3/#publishing-tripupdates","title":"Publishing TripUpdates","text":"<pre><code>from nibble.trip_updates_publisher import publish_trip_updates\n\nawait publish_trip_updates(\n    snapshot=snapshot,\n    gtfs=gtfs,\n    bucket=\"my-bucket\",\n    key=\"gtfs-rt/trip_updates.pb\",\n    region=\"us-east-1\",             # optional, defaults to us-east-1\n    agency_timezone=\"America/New_York\",  # optional, defaults to UTC\n)\n</code></pre> <p>For each vehicle with a resolved <code>trip_id</code> and <code>current_stop_sequence</code>, nibble:</p> <ol> <li>Computes the vehicle's current delay using <code>compute_delay</code> (see Arrival predictions)</li> <li>Builds a <code>TripUpdate</code> with a <code>StopTimeUpdate</code> for every remaining stop in the trip</li> <li>Sets <code>arrival.delay</code> and <code>departure.delay</code> on each stop to the computed delay</li> </ol> <p>Vehicles without a <code>trip_id</code>, <code>current_stop_sequence</code>, or computable delay are skipped silently.</p> <p>Pass <code>agency_timezone</code> to get correct delay values for agencies not in UTC (see Arrival predictions \u2014 Timezone handling).</p>"},{"location":"how-to/publishing-gtfs-rt-to-s3/#publishing-both-feeds-together","title":"Publishing both feeds together","text":"<p>In practice you'll want to publish both feeds on the same cadence as nibble's poll loop:</p> <pre><code>import asyncio\nfrom nibble.vehicle_positions_publisher import publish_vehicle_positions\nfrom nibble.trip_updates_publisher import publish_trip_updates\n\nBUCKET = \"my-bucket\"\nREGION = \"us-east-1\"\nAGENCY_TZ = \"America/New_York\"\n\nasync def on_poll(snapshot, gtfs):\n    await asyncio.gather(\n        publish_vehicle_positions(\n            snapshot=snapshot,\n            bucket=BUCKET,\n            key=\"gtfs-rt/vehicle_positions.pb\",\n            region=REGION,\n        ),\n        publish_trip_updates(\n            snapshot=snapshot,\n            gtfs=gtfs,\n            bucket=BUCKET,\n            key=\"gtfs-rt/trip_updates.pb\",\n            region=REGION,\n            agency_timezone=AGENCY_TZ,\n        ),\n    )\n</code></pre> <p>Both uploads run concurrently. Each is a single <code>PutObject</code> call that overwrites the previous file, so consumers always read the latest snapshot.</p>"},{"location":"how-to/trip-assignment-corrections/","title":"Manual trip assignment corrections","text":"<p>Sometimes nibble cannot automatically determine which trip a vehicle is running. This happens when:</p> <ul> <li>The GTFS-RT feed temporarily omits <code>trip_id</code> (common at terminals or during AVL glitches)</li> <li>Position inference has multiple plausible candidates and picks the wrong one</li> <li>A vehicle is reassigned mid-route and the feed hasn't caught up yet</li> </ul> <p>In these cases, an operator can issue a manual trip assignment correction via the API. Nibble applies it immediately on the next poll \u2014 overriding automatic inference \u2014 and auto-expires it once the vehicle reaches the final stop of the assigned trip.</p>"},{"location":"how-to/trip-assignment-corrections/#how-it-works","title":"How it works","text":"<p>Manual corrections slot into the resolution ladder as step 0, evaluated before any feed data or position inference:</p> <pre><code>0. Manual override present   \u2192 confidence=\"confirmed\", provenance=\"manual\"\n1. trip_id in feed + in GTFS \u2192 confidence=\"confirmed\", provenance=\"observed\"\n2. trip_id in feed, not GTFS \u2192 confidence=\"confirmed\", provenance=\"observed\" (warning logged)\n3. No trip_id, within stale threshold \u2192 confidence=\"inferred\", provenance=\"inferred\"\n4. No trip_id, beyond threshold       \u2192 confidence=\"stale\"\n</code></pre> <p>When an override is active, the resulting vehicle event carries:</p> <ul> <li><code>provenance: \"manual\"</code> \u2014 so downstream consumers know the assignment was operator-issued</li> <li><code>confidence: \"confirmed\"</code> \u2014 treated as authoritative by gobble and any other consumers</li> </ul> <p>Auto-expiry: On every poll, nibble checks the vehicle's current <code>stop_sequence</code> against the last stop of the assigned trip in static GTFS. Once the vehicle reaches or passes that stop, the override is silently removed and the resolution ladder resumes normal operation.</p>"},{"location":"how-to/trip-assignment-corrections/#typical-workflow","title":"Typical workflow","text":"<p>This is designed to work with a companion web app that subscribes to nibble's <code>/vehicles</code> SSE stream.</p> <ol> <li>The web app watches for vehicles where <code>confidence</code> is <code>\"inferred\"</code> or <code>\"stale\"</code> \u2014 these are candidates for manual correction.</li> <li>An operator selects the correct trip from a list and submits it.</li> <li>The web app <code>POST</code>s to nibble's <code>/trip_assignments</code> endpoint.</li> <li>On the next poll (within <code>NIBBLE_POLL_INTERVAL_SECONDS</code>), nibble applies the correction and broadcasts an <code>update</code> event with <code>provenance=\"manual\"</code>.</li> <li>When the vehicle completes the trip, nibble auto-expires the correction with no operator action required.</li> </ol>"},{"location":"how-to/trip-assignment-corrections/#api","title":"API","text":""},{"location":"how-to/trip-assignment-corrections/#create-or-replace-a-trip-assignment","title":"Create or replace a trip assignment","text":"<pre><code>POST /trip_assignments\nContent-Type: application/json\n</code></pre> <p>Request body:</p> <pre><code>{\n  \"vehicle_id\": \"BUS-42\",\n  \"trip_id\": \"trip-123\"\n}\n</code></pre> Field Type Description <code>vehicle_id</code> string The vehicle to correct. Must match the <code>id</code> field in SSE events. <code>trip_id</code> string The GTFS trip to assign. Must exist in the loaded static GTFS. <p>Response <code>200 OK</code>:</p> <pre><code>{\n  \"vehicle_id\": \"BUS-42\",\n  \"trip_id\": \"trip-123\",\n  \"assigned_at\": \"2024-06-01T12:34:56.789012+00:00\"\n}\n</code></pre> <p>Response <code>422 Unprocessable Entity</code> \u2014 if either field is missing, or if <code>trip_id</code> is not found in the current static GTFS:</p> <pre><code>{\n  \"error\": \"trip_id 'trip-999' not found in static GTFS\"\n}\n</code></pre> <p>Posting a correction for a <code>vehicle_id</code> that already has one replaces the existing assignment.</p>"},{"location":"how-to/trip-assignment-corrections/#list-active-corrections","title":"List active corrections","text":"<pre><code>GET /trip_assignments\n</code></pre> <p>Returns all currently active manual overrides.</p> <p>Response <code>200 OK</code>:</p> <pre><code>{\n  \"BUS-42\": {\n    \"trip_id\": \"trip-123\",\n    \"assigned_at\": \"2024-06-01T12:34:56.789012+00:00\"\n  },\n  \"BUS-99\": {\n    \"trip_id\": \"trip-456\",\n    \"assigned_at\": \"2024-06-01T11:00:00.000000+00:00\"\n  }\n}\n</code></pre> <p>An empty object <code>{}</code> is returned when no corrections are active.</p>"},{"location":"how-to/trip-assignment-corrections/#remove-a-correction","title":"Remove a correction","text":"<pre><code>DELETE /trip_assignments/{vehicle_id}\n</code></pre> <p>Removes the manual override for the given vehicle. The resolution ladder resumes normal operation on the next poll. No-op if no override exists.</p> <p>Response: <code>204 No Content</code></p>"},{"location":"how-to/trip-assignment-corrections/#persistence","title":"Persistence","text":"<p>Corrections survive nibble restarts. They are stored as a JSON file whose path defaults to <code>overrides.json</code> in the working directory and is configurable via the <code>NIBBLE_OVERRIDES_PATH</code> environment variable:</p> <pre><code>NIBBLE_OVERRIDES_PATH=/var/lib/nibble/overrides.json\n</code></pre> <p>Writes are atomic (write to a temp file + <code>os.replace</code>) so a crash mid-write cannot corrupt the store.</p>"},{"location":"how-to/trip-assignment-corrections/#example-integrating-from-python","title":"Example: integrating from Python","text":"<pre><code>import httpx\n\nNIBBLE_BASE = \"http://localhost:8080\"\n\n# Assign a trip\nresp = httpx.post(\n    f\"{NIBBLE_BASE}/trip_assignments\",\n    json={\"vehicle_id\": \"BUS-42\", \"trip_id\": \"trip-123\"},\n)\nresp.raise_for_status()\nprint(resp.json())\n# {'vehicle_id': 'BUS-42', 'trip_id': 'trip-123', 'assigned_at': '...'}\n\n# Check active corrections\nresp = httpx.get(f\"{NIBBLE_BASE}/trip_assignments\")\nprint(resp.json())\n\n# Remove early if needed\nhttpx.delete(f\"{NIBBLE_BASE}/trip_assignments/BUS-42\")\n</code></pre>"},{"location":"how-to/trip-assignment-corrections/#filtering-by-provenance-in-consumers","title":"Filtering by provenance in consumers","text":"<p>Downstream consumers (e.g. gobble) can filter or annotate events by <code>provenance</code> to distinguish manual corrections from feed data:</p> <pre><code>source.addEventListener(\"update\", (e) =&gt; {\n  const vehicle = JSON.parse(e.data);\n  const { provenance, confidence } = vehicle.attributes;\n\n  if (provenance === \"manual\") {\n    // Trip was operator-assigned \u2014 show a badge or note in the UI\n  } else if (confidence === \"inferred\") {\n    // Trip is nibble's best guess \u2014 may want to flag for review\n  }\n});\n</code></pre>"},{"location":"reference/adapters/","title":"Adapters","text":"<p>Feed adapter plugins that fetch vehicle data and return a <code>FeedMessage</code>.</p>"},{"location":"reference/adapters/#factory","title":"Factory","text":""},{"location":"reference/adapters/#nibble.adapters","title":"<code>nibble.adapters</code>","text":"<p>Feed adapter factory.</p>"},{"location":"reference/adapters/#nibble.adapters.get_adapter","title":"<code>get_adapter(adapter_name, url, agency_id='', agency_timezone=None, auth_type='none', auth_secret=None, passio_static_routes_file=None)</code>","text":"<p>Return the appropriate adapter for the given adapter name.</p> <p>Parameters:</p> Name Type Description Default <code>adapter_name</code> <code>str</code> <p>One of <code>\"gtfs_rt\"</code> or <code>\"passio\"</code>.</p> required <code>url</code> <code>str</code> <p>The feed URL (GTFS-RT endpoint or Passio GO! API URL).</p> required <code>agency_id</code> <code>str</code> <p>Optional agency identifier used by some JSON adapters.</p> <code>''</code> <code>agency_timezone</code> <code>str | None</code> <p>IANA timezone name used to localise naive timestamps from adapters that report local time without a UTC offset.</p> <code>None</code> <code>auth_type</code> <code>str</code> <p>Auth method - <code>\"none\"</code>, <code>\"query_param\"</code>, <code>\"header\"</code>, or <code>\"path\"</code>. Only <code>\"path\"</code> affects the URL here; the others are handled by the httpx client via <code>build_httpx_auth</code>.</p> <code>'none'</code> <code>auth_secret</code> <code>str | None</code> <p>The API key or token value used for <code>\"path\"</code> substitution.</p> <code>None</code> <p>Returns:</p> Type Description <code>BaseAdapter</code> <p>A <code>BaseAdapter</code> instance ready to use in the poll loop.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>adapter_name</code> does not match a known adapter.</p>"},{"location":"reference/adapters/#base-class","title":"Base class","text":""},{"location":"reference/adapters/#nibble.adapters.base","title":"<code>nibble.adapters.base</code>","text":"<p>Abstract base class for feed adapters.</p>"},{"location":"reference/adapters/#nibble.adapters.base.BaseAdapter","title":"<code>BaseAdapter</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Fetches vehicle data and returns it as a GTFS-RT FeedMessage.</p> <p>Adapters decouple the poll loop from the wire format of the upstream feed. GTFS-RT feeds use <code>GtfsRtAdapter</code>; proprietary JSON APIs (e.g. Passio GO!) use a format-specific adapter that converts to FeedMessage before the normalizer sees the data.</p>"},{"location":"reference/adapters/#nibble.adapters.base.BaseAdapter.fetch","title":"<code>fetch(client)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Fetch vehicles and return as a FeedMessage, or None on error.</p> <p>Implementations should catch all network and parse errors internally and return <code>None</code> so the poll loop can skip the cycle gracefully.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>AsyncClient</code> <p>Shared async HTTP client for the current poll cycle.</p> required <p>Returns:</p> Type Description <code>FeedMessage | None</code> <p>A parsed <code>FeedMessage</code> containing vehicle positions, or <code>None</code></p> <code>FeedMessage | None</code> <p>if the fetch or parse failed.</p>"},{"location":"reference/adapters/#gtfs-rt","title":"GTFS-RT","text":""},{"location":"reference/adapters/#nibble.adapters.gtfs_rt","title":"<code>nibble.adapters.gtfs_rt</code>","text":"<p>GTFS-RT adapter - wraps the existing fetch_feed() function.</p>"},{"location":"reference/adapters/#nibble.adapters.gtfs_rt.GtfsRtAdapter","title":"<code>GtfsRtAdapter</code>","text":"<p>               Bases: <code>BaseAdapter</code></p> <p>Fetches a standard GTFS-RT VehiclePositions protobuf feed.</p>"},{"location":"reference/adapters/#nibble.adapters.gtfs_rt.GtfsRtAdapter.__init__","title":"<code>__init__(url)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>GTFS-RT VehiclePositions endpoint URL.</p> required"},{"location":"reference/adapters/#nibble.adapters.gtfs_rt.GtfsRtAdapter.fetch","title":"<code>fetch(client)</code>  <code>async</code>","text":"<p>Fetch and return the GTFS-RT feed, delegating to :func:<code>~nibble.gtfs.realtime.fetch_feed</code>.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>AsyncClient</code> <p>Shared async HTTP client.</p> required <p>Returns:</p> Type Description <code>FeedMessage | None</code> <p>A parsed <code>FeedMessage</code>, or <code>None</code> on error.</p>"},{"location":"reference/adapters/#mwrta","title":"MWRTA","text":""},{"location":"reference/adapters/#nibble.adapters.mwrta","title":"<code>nibble.adapters.mwrta</code>","text":"<p>MWRTA JSON API adapter - converts to GTFS-RT FeedMessage.</p> <p>MWRTA returns an array of vehicle objects from its REST API. This adapter translates each object into a GTFS-RT VehiclePosition entity.</p> <p>Expected JSON shape (fields used by this adapter):     [       {         \"ID\": 979666956,         \"Route\": \"RT14\",         \"Destination\": null,         \"Lat\": 42.2763303,         \"Long\": -71.4119646,         \"Speed\": 7.175,         \"Heading\": 39.12,         \"DateTime\": \"2026-03-08T18:33:59\",         \"VehiclePlate\": \"205\",         \"Active\": true,         ...       },       ...     ]</p> <p>Fields that are absent, null, or inactive are omitted or skipped.</p>"},{"location":"reference/adapters/#nibble.adapters.mwrta.MwrtaAdapter","title":"<code>MwrtaAdapter</code>","text":"<p>               Bases: <code>BaseAdapter</code></p> <p>Fetches MWRTA JSON vehicle data and converts it to a FeedMessage.</p>"},{"location":"reference/adapters/#nibble.adapters.mwrta.MwrtaAdapter.__init__","title":"<code>__init__(url, agency_id='', agency_timezone=None)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>MWRTA REST API endpoint URL.</p> required <code>agency_id</code> <code>str</code> <p>Unused; kept for interface compatibility.</p> <code>''</code> <code>agency_timezone</code> <code>str | None</code> <p>IANA timezone name (e.g. <code>\"America/New_York\"</code>) used to interpret naive <code>DateTime</code> values.  Defaults to UTC.</p> <code>None</code>"},{"location":"reference/adapters/#nibble.adapters.mwrta.MwrtaAdapter.fetch","title":"<code>fetch(client)</code>  <code>async</code>","text":"<p>GET the MWRTA vehicle list and convert it to a GTFS-RT FeedMessage.</p> <p>Inactive vehicles (<code>Active == false</code>) are skipped. <code>DateTime</code> values are parsed as local time using the configured agency timezone.</p> <p>Returns:</p> Type Description <code>FeedMessage | None</code> <p>A FeedMessage containing one entity per active vehicle, or None on error.</p>"},{"location":"reference/adapters/#mylirr","title":"Mylirr","text":""},{"location":"reference/adapters/#nibble.adapters.mylirr","title":"<code>nibble.adapters.mylirr</code>","text":"<p>MTA Railroad locations adapter using the backend-unified.mylirr.org API.</p> <p>Fetches real-time train positions for LIRR (and optionally MNR) from the MTA Radar backend. The API returns a JSON array of train objects, each with GPS coordinates, speed, heading, and status. Train numbers map to <code>trip_short_name</code> in the static GTFS and are rewritten by the <code>MtaRailroadNormalizer</code> before downstream processing.</p> <p>Expected response shape (per train):     {       \"train_num\": \"1656\",       \"realtime\": true,       \"location\": {         \"latitude\": 40.734199,         \"longitude\": -73.666574,         \"heading\": 72.4,         \"speed\": 51.4,          # mph         \"timestamp\": 1775247740       },       \"status\": {         \"canceled\": false,         ...       },       \"details\": {         \"stops\": [           {\"code\": \"0NY\", \"stop_status\": \"DEPARTED\", ...},           {\"code\": \"0HL\", \"stop_status\": \"SCHEDULED\", ...},           ...         ]       }     }</p>"},{"location":"reference/adapters/#nibble.adapters.mylirr.MyLirrAdapter","title":"<code>MyLirrAdapter</code>","text":"<p>               Bases: <code>BaseAdapter</code></p> <p>Fetches MTA Railroad train positions from backend-unified.mylirr.org.</p>"},{"location":"reference/adapters/#nibble.adapters.mylirr.MyLirrAdapter.__init__","title":"<code>__init__(url)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>Full API URL, e.g. <code>https://backend-unified.mylirr.org/locations?geometry=TRACK_TURF&amp;railroad=LIRR</code></p> required"},{"location":"reference/adapters/#nibble.adapters.mylirr.MyLirrAdapter.fetch","title":"<code>fetch(client)</code>  <code>async</code>","text":"<p>GET the locations endpoint and convert the response to a FeedMessage.</p> <p>Trains with no realtime location or a canceled status are skipped.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>AsyncClient</code> <p>Shared async HTTP client.</p> required <p>Returns:</p> Type Description <code>FeedMessage | None</code> <p>A <code>FeedMessage</code> containing one VehiclePosition entity per active</p> <code>FeedMessage | None</code> <p>train, or <code>None</code> on network or parse error.</p>"},{"location":"reference/adapters/#passio-go","title":"Passio GO!","text":""},{"location":"reference/adapters/#nibble.adapters.passio","title":"<code>nibble.adapters.passio</code>","text":"<p>Passio GO! JSON API adapter - converts to GTFS-RT FeedMessage.</p> <p>Passio GO! returns vehicle data from a POST endpoint. This adapter posts {\"s0\": system_id, \"sA\": 1} to mapGetData.php?getBuses=2 and translates the response into a GTFS-RT VehiclePosition FeedMessage.</p> Expected JSON shape <p>{   \"buses\": {     \"\": [       {         \"busId\": \"101\",         \"routeId\": \"R1\",         \"tripId\": \"T123\",          # may be null         \"latitude\": 42.3601,         \"longitude\": -71.0589,         \"calculatedCourse\": 270,   # may be null         \"speed\": 12.5,             # may be null       }     ],     ...   } } <p>The vehicle ID \"-1\" is a sentinel used by PassioGO for system metadata and is skipped.</p> <p>The adapter also fetches the Passio routes endpoint on startup and periodically thereafter, building a <code>myid \u2192 display_name</code> mapping from the <code>all[]</code> array:</p> <pre><code>{\n  \"all\": [\n    {\n      \"myid\": \"50075\",\n      \"shortName\": \"4\",     # GTFS route_short_name; null for named routes\n      \"name\": \"Fitchburg 9 &amp; 8\",\n      ...\n    },\n    ...\n  ]\n}\n</code></pre> <p>When the map is populated, <code>routeId</code> values in the vehicle feed are replaced with the corresponding <code>shortName</code> (preferred) or <code>name</code>, so downstream normalizers and the state machine see GTFS-compatible identifiers.</p>"},{"location":"reference/adapters/#nibble.adapters.passio.PassioAdapter","title":"<code>PassioAdapter</code>","text":"<p>               Bases: <code>BaseAdapter</code></p> <p>Fetches Passio GO! vehicle data via POST and converts it to a FeedMessage.</p>"},{"location":"reference/adapters/#nibble.adapters.passio.PassioAdapter.__init__","title":"<code>__init__(system_id, agency_id='', static_routes_file=None)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>system_id</code> <code>str</code> <p>PassioGO system ID (e.g. \"2046\" for BAT).</p> required <code>agency_id</code> <code>str</code> <p>Unused; kept for interface compatibility.</p> <code>''</code> <code>static_routes_file</code> <code>str | None</code> <p>Path to a JSON file with the same <code>{\"all\": [...]}</code> structure as the Passio routes endpoint.  Loaded on startup as an initial fallback; the live endpoint will overwrite it on the first successful fetch.</p> <code>None</code>"},{"location":"reference/adapters/#nibble.adapters.passio.PassioAdapter.fetch","title":"<code>fetch(client)</code>  <code>async</code>","text":"<p>POST to PassioGO and convert the response to a GTFS-RT FeedMessage.</p> <p>Fetches (or refreshes) the routes map before the first poll and every 24 hours thereafter, then translates <code>routeId</code> values to GTFS-compatible route names.</p> <p>Returns:</p> Type Description <code>FeedMessage | None</code> <p>A FeedMessage built from the buses dict, or None on error.</p>"},{"location":"reference/adapters/#routematch","title":"RouteMatch","text":""},{"location":"reference/adapters/#nibble.adapters.routematch","title":"<code>nibble.adapters.routematch</code>","text":"<p>RouteMatch JSON API adapter - converts to GTFS-RT FeedMessage.</p> <p>RouteMatch returns a JSON object with a <code>data</code> array from its REST API. This adapter translates each object into a GTFS-RT VehiclePosition entity.</p> <p>Expected JSON shape (fields used by this adapter):     {       \"data\": [         {           \"vehicleId\": \"2404\",           \"latitude\": 42.638,           \"longitude\": -73.112,           \"heading\": 191,           \"speed\": 40,           \"masterRouteId\": \"Wk Rt 01\",           \"tripId\": \"Rte 01 1130 in\",           \"lastUpdate\": \"2026-03-18T11:35:00.000-04:00\",           \"deadhead\": false,           ...         },         ...       ]     }</p> <p><code>speed</code> is assumed to be in mph and is converted to m/s. <code>heading</code> may be null. Deadheading vehicles are skipped. Fields that are absent or null are omitted from the protobuf message.</p>"},{"location":"reference/adapters/#nibble.adapters.routematch.RouteMatchAdapter","title":"<code>RouteMatchAdapter</code>","text":"<p>               Bases: <code>BaseAdapter</code></p> <p>Fetches RouteMatch JSON vehicle data and converts it to a FeedMessage.</p>"},{"location":"reference/adapters/#nibble.adapters.routematch.RouteMatchAdapter.__init__","title":"<code>__init__(url, agency_id='')</code>","text":"<p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>RouteMatch REST API endpoint URL.</p> required <code>agency_id</code> <code>str</code> <p>Unused; kept for interface compatibility.</p> <code>''</code>"},{"location":"reference/adapters/#nibble.adapters.routematch.RouteMatchAdapter.fetch","title":"<code>fetch(client)</code>  <code>async</code>","text":"<p>GET the RouteMatch vehicle data and convert it to a GTFS-RT FeedMessage.</p> <p>Deadheading vehicles are skipped. Speed values (<code>speed</code>) are converted from mph to m/s; implausible values (&gt; 100 mph) are dropped. Falls back to the feed header timestamp when <code>lastUpdate</code> is absent or unparseable.</p> <p>Returns:</p> Type Description <code>FeedMessage | None</code> <p>A FeedMessage containing one entity per non-deadheading vehicle, or None on error.</p>"},{"location":"reference/adapters/#swiv","title":"Swiv","text":""},{"location":"reference/adapters/#nibble.adapters.swiv","title":"<code>nibble.adapters.swiv</code>","text":"<p>Swiv JSON API adapter - converts to GTFS-RT FeedMessage.</p> <p>Swiv returns a JSON object with a <code>vehicule</code> array from its REST API. This adapter translates each object into a GTFS-RT VehiclePosition entity.</p> <p>Expected JSON shape (fields used by this adapter):     {       \"vehicule\": [         {           \"id\": 2048,           \"numeroEquipement\": \"1605\",           \"type\": \"Bus\",           \"localisation\": {             \"lat\": 42.623,             \"lng\": -71.362,             \"cap\": 223           },           \"conduite\": {             \"idLigne\": 27298,             \"vitesse\": 4,             \"destination\": \"Westford Street/Drum Hill\"           }         },         ...       ]     }</p> <p>When a <code>topo_url</code> is provided, the adapter also fetches the Swiv topology endpoint on startup and periodically thereafter, parsing the <code>idLigne \u2192 nomCommercial</code> mapping from the <code>topo[].ligne[]</code> array:</p> <pre><code>{\n  \"topo\": [\n    {\n      \"ligne\": [\n        {\n          \"idLigne\": 27298,\n          \"nomCommercial\": \"14\",\n          ...\n        },\n        ...\n      ]\n    },\n    ...\n  ]\n}\n</code></pre> <p>When the map is populated, <code>idLigne</code> values in the vehicle feed are replaced with the corresponding <code>nomCommercial</code> before the FeedMessage is returned, so downstream normalizers and the state machine see human-readable route IDs.</p> <p><code>vitesse</code> is assumed to be in km/h and is converted to m/s. Fields that are absent or null are omitted from the protobuf message.</p>"},{"location":"reference/adapters/#nibble.adapters.swiv.SwivAdapter","title":"<code>SwivAdapter</code>","text":"<p>               Bases: <code>BaseAdapter</code></p> <p>Fetches Swiv JSON vehicle data and converts it to a FeedMessage.</p>"},{"location":"reference/adapters/#nibble.adapters.swiv.SwivAdapter.__init__","title":"<code>__init__(url, agency_id='', topo_url=None)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>Swiv REST API endpoint URL.</p> required <code>agency_id</code> <code>str</code> <p>Unused; kept for interface compatibility.</p> <code>''</code> <code>topo_url</code> <code>str | None</code> <p>Swiv topology endpoint URL. When <code>None</code>, derived automatically by stripping <code>/vehicules</code> from <code>url</code> if present (the standard Swiv URL convention).</p> <code>None</code>"},{"location":"reference/adapters/#nibble.adapters.swiv.SwivAdapter.fetch","title":"<code>fetch(client)</code>  <code>async</code>","text":"<p>GET the Swiv vehicle data and convert it to a GTFS-RT FeedMessage.</p> <p>Appends a <code>_tmp</code> millisecond timestamp to the URL to bust caches. Speed values (<code>vitesse</code>) are converted from km/h to m/s; implausible values (&gt; 150 km/h) are dropped.</p> <p>If a <code>topo_url</code> was provided, the topo mapping is refreshed on the first call and every 24 hours thereafter; <code>idLigne</code> values are replaced with the corresponding <code>nomCommercial</code> in the returned feed.</p> <p>Returns:</p> Type Description <code>FeedMessage | None</code> <p>A FeedMessage containing one entity per vehicle, or None on error.</p>"},{"location":"reference/adapters/#trillium","title":"Trillium","text":""},{"location":"reference/adapters/#nibble.adapters.trillium","title":"<code>nibble.adapters.trillium</code>","text":"<p>Trillium Transit JSON API adapter - converts to GTFS-RT FeedMessage.</p> <p>Trillium returns a JSON object with a top-level <code>data</code> array of vehicle objects. This adapter translates each object into a GTFS-RT VehiclePosition entity.</p> <p>Expected JSON shape (fields used by this adapter):     {       \"status\": \"success\",       \"data\": [         {           \"id\": 8819,           \"name\": \"1204\",           \"lat\": 42.76554,           \"lon\": -71.09184,           \"speed\": 0,           \"headingDegrees\": 314,           \"lastUpdated\": \"2026-03-07T23:52:57Z\",           \"route_id\": \"10729\",           \"route_short_name\": \"16\",           \"vehicleType\": \"bus\",           ...         },         ...       ]     }</p> <p>Fields that are absent or null are omitted from the protobuf message.</p>"},{"location":"reference/adapters/#nibble.adapters.trillium.TrilliumAdapter","title":"<code>TrilliumAdapter</code>","text":"<p>               Bases: <code>BaseAdapter</code></p> <p>Fetches Trillium Transit JSON vehicle data and converts it to a FeedMessage.</p>"},{"location":"reference/adapters/#nibble.adapters.trillium.TrilliumAdapter.__init__","title":"<code>__init__(url, agency_id='')</code>","text":"<p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>Trillium Transit JSON API endpoint URL.</p> required <code>agency_id</code> <code>str</code> <p>Unused; kept for interface compatibility.</p> <code>''</code>"},{"location":"reference/adapters/#nibble.adapters.trillium.TrilliumAdapter.fetch","title":"<code>fetch(client)</code>  <code>async</code>","text":"<p>GET the Trillium vehicle data and convert it to a GTFS-RT FeedMessage.</p> <p>Extracts vehicles from the top-level <code>data</code> array. <code>lastUpdated</code> timestamps are parsed as ISO 8601 UTC.</p> <p>Returns:</p> Type Description <code>FeedMessage | None</code> <p>A FeedMessage containing one entity per vehicle, or None on error.</p>"},{"location":"reference/adapters/#vta","title":"VTA","text":""},{"location":"reference/adapters/#nibble.adapters.vta","title":"<code>nibble.adapters.vta</code>","text":"<p>VTA (Vineyard Transit Authority) JSON API adapter - converts to GTFS-RT FeedMessage.</p> <p>VTA's MyTransitRide API returns a JSON array of vehicle objects. The request URL must include the <code>patternIds</code> query parameter listing the route patterns to fetch; this is supplied as the configured feed URL.</p> <p>Expected JSON shape (fields used by this adapter):     [       {         \"vehicleId\": 22,         \"name\": \"103\",         \"patternId\": 1401,         \"headsignText\": \"3\",         \"lat\": 41.455,         \"lng\": -70.601,         \"velocity\": 0,         \"bearing\": 279,         \"lastUpdate\": \"2026-03-18T15:46:58\",         \"vehicleStateId\": 1       },       ...     ]</p> <p><code>velocity</code> is assumed to be in mph and is converted to m/s. <code>lastUpdate</code> is a naive local datetime; <code>agency_timezone</code> is required to interpret it. Fields that are absent or null are omitted from the protobuf message.</p>"},{"location":"reference/adapters/#nibble.adapters.vta.VtaAdapter","title":"<code>VtaAdapter</code>","text":"<p>               Bases: <code>BaseAdapter</code></p> <p>Fetches VTA MyTransitRide JSON vehicle data and converts it to a FeedMessage.</p>"},{"location":"reference/adapters/#nibble.adapters.vta.VtaAdapter.__init__","title":"<code>__init__(url, agency_id='', agency_timezone=None)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>VTA MyTransitRide API endpoint URL (must include <code>patternIds</code> query param).</p> required <code>agency_id</code> <code>str</code> <p>Unused; kept for interface compatibility.</p> <code>''</code> <code>agency_timezone</code> <code>str | None</code> <p>IANA timezone name (e.g. <code>\"America/New_York\"</code>) used to interpret naive <code>lastUpdate</code> values.  Defaults to UTC.</p> <code>None</code>"},{"location":"reference/adapters/#nibble.adapters.vta.VtaAdapter.fetch","title":"<code>fetch(client)</code>  <code>async</code>","text":"<p>GET the VTA vehicle data and convert it to a GTFS-RT FeedMessage.</p> <p>Speed values (<code>velocity</code>) are converted from mph to m/s. <code>lastUpdate</code> is a naive local datetime interpreted with the configured agency timezone.</p> <p>Returns:</p> Type Description <code>FeedMessage | None</code> <p>A FeedMessage containing one entity per vehicle, or None on error.</p>"},{"location":"reference/config/","title":"Config","text":"<p>Application configuration loaded from <code>NIBBLE_*</code> environment variables.</p>"},{"location":"reference/config/#nibble.config","title":"<code>nibble.config</code>","text":"<p>Pydantic Settings loaded from NIBBLE_* environment variables or a .env file.</p>"},{"location":"reference/config/#nibble.config.Settings","title":"<code>Settings</code>","text":"<p>               Bases: <code>BaseSettings</code></p> <p>Application configuration.</p> <p>All fields are read from environment variables prefixed with <code>NIBBLE_</code> (e.g. <code>NIBBLE_GTFS_RT_URL</code>). A <code>.env</code> file in the working directory is also supported.</p> <p>Attributes:</p> Name Type Description <code>gtfs_rt_url</code> <code>str</code> <p>URL of the GTFS-RT protobuf endpoint. Required.</p> <code>gtfs_static_url</code> <code>str</code> <p>URL of the static GTFS ZIP archive. Required.</p> <code>poll_interval_seconds</code> <code>int</code> <p>How often to fetch the feed, in seconds. Defaults to <code>15</code>.</p> <code>stale_vehicle_threshold_seconds</code> <code>int</code> <p>Seconds after which a vehicle with no <code>trip_id</code> is considered stale and removed. Defaults to <code>90</code>.</p> <code>normalizer</code> <code>str</code> <p>Name of the feed normalizer plugin - <code>\"default\"</code>, <code>\"ripta\"</code>, or <code>\"mwrta\"</code>. Defaults to <code>\"default\"</code>.</p> <code>max_interpolation_stops</code> <code>int</code> <p>Maximum stop gap size to interpolate. Larger gaps are left as-is. Defaults to <code>3</code>.</p> <code>host</code> <code>str</code> <p>Bind address for the HTTP server. Defaults to <code>\"0.0.0.0\"</code>.</p> <code>port</code> <code>int</code> <p>Listen port for the HTTP server. Defaults to <code>8080</code>.</p> <code>adapter</code> <code>str</code> <p>Feed adapter to use - <code>\"gtfs_rt\"</code> (default), <code>\"passio\"</code>, <code>\"mwrta\"</code>, <code>\"trillium\"</code>, or <code>\"swiv\"</code>.</p> <code>agency_id</code> <code>str</code> <p>Agency identifier used by JSON adapters such as Passio GO!. Defaults to <code>\"\"</code>.</p> <code>agency_timezone</code> <code>str | None</code> <p>IANA timezone name for the agency (e.g. <code>\"America/New_York\"</code>).  When set, trip inference from position filters candidates by their scheduled time window before ranking by geometry, improving accuracy on routes with multiple trips sharing the same shape.  Defaults to <code>None</code> (geometry-only).</p> <code>gtfs_static_fix</code> <code>bool</code> <p>When <code>True</code>, download the raw static feed, apply CSV fixers, publish the corrected ZIP to S3, and load from the fixed bytes. Requires <code>s3_bucket</code>. Defaults to <code>False</code>.</p> <code>s3_bucket</code> <code>str | None</code> <p>S3 bucket name for publishing fixed GTFS. Required when <code>gtfs_static_fix</code> is <code>True</code>.</p> <code>s3_agency_slug</code> <code>str | None</code> <p>Optional slug prepended to both <code>s3_prefix</code> and <code>s3_archived_feeds_key</code>, allowing multiple agencies to share one bucket. E.g. <code>\"mwrta\"</code> \u2192 keys become <code>mwrta/gtfs/\u2026</code> and <code>mwrta/archived_feeds.txt</code>. Defaults to <code>None</code>.</p> <code>s3_prefix</code> <code>str</code> <p>S3 key prefix for the published GTFS ZIP. Defaults to <code>\"gtfs\"</code>.</p> <code>s3_archived_feeds_key</code> <code>str</code> <p>S3 key for the archived feeds index file. Defaults to <code>\"archived_feeds.txt\"</code>.</p> <code>s3_region</code> <code>str</code> <p>AWS region for S3 operations. Defaults to <code>\"us-east-1\"</code>.</p> <code>gtfs_reload_interval_hours</code> <code>int | None</code> <p>How often to check for a new static GTFS bundle, in hours. When set, a background loop re-downloads the feed and reloads the in-memory indexes if <code>feed_start_date</code> has changed. Defaults to <code>None</code> (no reload).</p> <code>enable_sse</code> <code>bool</code> <p>When <code>True</code> (the default), start the FastAPI HTTP server and serve SSE vehicle-event streams. Set to <code>False</code> to run in a headless polling-only mode (useful when the only output is S3 VehiclePositions). Defaults to <code>True</code>.</p> <code>publish_vehicle_positions</code> <code>bool</code> <p>When <code>True</code>, serialize the current vehicle snapshot to a GTFS-RT <code>VehiclePositions</code> protobuf and upload it to S3 after every successful poll. Requires <code>s3_bucket</code>. Defaults to <code>False</code>.</p> <code>vehicle_positions_s3_key</code> <code>str</code> <p>S3 object key for the published <code>VehiclePositions</code> protobuf. Defaults to <code>\"vehicle_positions.pb\"</code>.</p> <code>publish_trip_updates</code> <code>bool</code> <p>When <code>True</code>, serialize stop-time predictions for all active vehicles to a GTFS-RT <code>TripUpdates</code> protobuf and upload it to S3 after every successful poll. Requires <code>s3_bucket</code>. Defaults to <code>False</code>.</p> <code>trip_updates_s3_key</code> <code>str</code> <p>S3 object key for the published <code>TripUpdates</code> protobuf. Defaults to <code>\"trip_updates.pb\"</code>.</p> <code>overrides_path</code> <code>Path</code> <p>Path to the JSON file used to persist manual trip assignment overrides across restarts. Defaults to <code>\"overrides.json\"</code> in the working directory.</p>"},{"location":"reference/config/#nibble.config.Settings.gtfs_static_inner_zip","title":"<code>gtfs_static_inner_zip = None</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>When set, the static GTFS URL is expected to return a zip-of-zips outer archive; this names the inner zip member to extract and use as the actual GTFS feed (e.g. <code>\"google_rail.zip\"</code> for SEPTA's <code>gtfs_public.zip</code>). Defaults to <code>None</code> (URL returns GTFS directly).</p>"},{"location":"reference/config/#nibble.config.Settings.ignore_unknown_trip_ids","title":"<code>ignore_unknown_trip_ids = False</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>When <code>True</code>, trip IDs from the real-time feed that are not present in the static GTFS are discarded rather than passed through.  Useful when the real-time source (e.g. PassioGO) uses an internal trip ID namespace that does not match the static feed, so position inference runs instead.</p>"},{"location":"reference/config/#nibble.config.Settings.fill_shape_dist_traveled","title":"<code>fill_shape_dist_traveled = True</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>When <code>True</code> (the default), back-fill <code>shape_dist_traveled</code> for any stop times that lack it by projecting each stop onto its trip's shape polyline.  Set to <code>False</code> for feeds (e.g. NYCT subway) that already include complete <code>shape_dist_traveled</code> values, to skip the projection step and reduce startup memory usage.</p>"},{"location":"reference/config/#nibble.config.Settings.passio_static_routes_file","title":"<code>passio_static_routes_file = None</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Path to a static JSON routes file (<code>{\"all\": [...]}</code> format) used by the Passio adapter as a fallback when the live routes endpoint is inaccessible.  Loaded once at startup.</p>"},{"location":"reference/config/#nibble.config.Settings.auth_type","title":"<code>auth_type = 'none'</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Auth method: <code>\"none\"</code> (default), <code>\"query_param\"</code>, <code>\"header\"</code>, or <code>\"path\"</code>.</p>"},{"location":"reference/config/#nibble.config.Settings.auth_secret","title":"<code>auth_secret = None</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>The API key or token value. Always supply via environment variable.</p>"},{"location":"reference/config/#nibble.config.Settings.auth_param_name","title":"<code>auth_param_name = 'api_key'</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Query parameter name when <code>auth_type=\"query_param\"</code>.</p>"},{"location":"reference/config/#nibble.config.Settings.auth_header_name","title":"<code>auth_header_name = 'X-API-Key'</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Header name when <code>auth_type=\"header\"</code>.</p>"},{"location":"reference/config/#nibble.config.Settings.infer_in_transit_from_trip_updates","title":"<code>infer_in_transit_from_trip_updates = False</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>When <code>True</code>, snapshot each trip's <code>stop_time_update</code> list between polls and synthesize a <code>current_status = IN_TRANSIT_TO</code> transition when the head stop drops, indicating the vehicle has departed. Useful for feeds (e.g. NYCT) that bundle TripUpdates but don't report <code>IN_TRANSIT_TO</code> on VehiclePositions.</p>"},{"location":"reference/config/#nibble.config.Settings.stalled_vehicle_timestamp_threshold_seconds","title":"<code>stalled_vehicle_timestamp_threshold_seconds = 90</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>When inferring departures from TripUpdates, suppress the event if the vehicle's timestamp is more than this many seconds behind the feed header timestamp. A stalled VehiclePosition makes list changes likely to be data artifacts rather than real departures.</p>"},{"location":"reference/emitter/","title":"Emitter","text":"<p>Serializes internal models to MBTA V3 JSON:API wire format.</p>"},{"location":"reference/emitter/#nibble.emitter","title":"<code>nibble.emitter</code>","text":"<p>Serializes internal models to MBTA V3 JSON:API wire format for SSE.</p>"},{"location":"reference/emitter/#nibble.emitter.to_mbta_v3","title":"<code>to_mbta_v3(event)</code>","text":"<p>Translate a VehicleEvent into an MBTA V3 JSON:API vehicle resource object.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>VehicleEvent</code> <p>The vehicle event to serialize.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dict with <code>\"id\"</code>, <code>\"type\"</code>, <code>\"attributes\"</code>, and <code>\"relationships\"</code></p> <code>dict[str, Any]</code> <p>keys conforming to the MBTA V3 API vehicle resource shape. Relationship</p> <code>dict[str, Any]</code> <p>data is <code>None</code> when the corresponding ID is unknown.</p>"},{"location":"reference/gtfs/","title":"GTFS Utilities","text":"<p>Modules for loading, fetching, fixing, and publishing GTFS data.</p>"},{"location":"reference/gtfs/#static-loader","title":"Static loader","text":""},{"location":"reference/gtfs/#nibble.gtfs.static","title":"<code>nibble.gtfs.static</code>","text":"<p>Downloads and parses static GTFS ZIP archives into in-memory indexes.</p>"},{"location":"reference/gtfs/#nibble.gtfs.static.StaticGTFS","title":"<code>StaticGTFS</code>  <code>dataclass</code>","text":"<p>In-memory indexes built from a static GTFS ZIP at startup.</p> <p>Attributes:</p> Name Type Description <code>trips</code> <code>dict[str, Trip]</code> <p>Mapping of <code>trip_id</code> \u2192 :class:<code>~nibble.models.Trip</code>, loaded from <code>trips.txt</code>.</p> <code>stop_times</code> <code>dict[str, list[StopTime]]</code> <p>Mapping of <code>trip_id</code> \u2192 list of :class:<code>~nibble.models.StopTime</code> sorted by <code>stop_sequence</code>, loaded from <code>stop_times.txt</code>.</p> <code>stops</code> <code>dict[str, tuple[float, float]]</code> <p>Mapping of <code>stop_id</code> \u2192 <code>(lat, lon)</code> float tuple, loaded from <code>stops.txt</code>.</p> <code>shapes</code> <code>dict[str, list[tuple[float, float]]]</code> <p>Mapping of <code>shape_id</code> \u2192 list of <code>(lat, lon)</code> tuples sorted by <code>shape_pt_sequence</code>, loaded from <code>shapes.txt</code>.</p> <code>route_trips</code> <code>dict[str, list[str]]</code> <p>Mapping of <code>route_id</code> \u2192 list of <code>trip_id</code> strings for all trips on that route.  Built from <code>trips.txt</code> at load time.</p> <code>route_short_names</code> <code>dict[str, str]</code> <p>Mapping of <code>route_short_name</code> \u2192 <code>route_id</code> for agencies whose GTFS-RT feeds report short names instead of the internal route UUID/ID.  Built from <code>routes.txt</code> at load time.</p> <code>trip_short_names</code> <code>dict[str, str]</code> <p>Mapping of <code>trip_short_name</code> \u2192 <code>trip_id</code> for agencies whose GTFS-RT feeds report human-readable train/trip numbers instead of the internal composite trip ID.  Built from <code>trips.txt</code> at load time; empty when <code>trip_short_name</code> is absent from the feed.</p>"},{"location":"reference/gtfs/#nibble.gtfs.static.last_stop_sequence","title":"<code>last_stop_sequence(gtfs, trip_id)</code>","text":"<p>Return the stop_sequence of the final stop for trip_id, or <code>None</code> if unknown.</p> <p>Parameters:</p> Name Type Description Default <code>gtfs</code> <code>StaticGTFS</code> <p>The loaded static GTFS indexes.</p> required <code>trip_id</code> <code>str</code> <p>The GTFS trip identifier to look up.</p> required <p>Returns:</p> Type Description <code>int | None</code> <p>The highest <code>stop_sequence</code> value for the trip, or <code>None</code> if the</p> <code>int | None</code> <p>trip has no stop-time data.</p>"},{"location":"reference/gtfs/#nibble.gtfs.static.extract_inner_zip","title":"<code>extract_inner_zip(content, inner_name)</code>","text":"<p>Return the bytes of <code>inner_name</code> from a zip-of-zips outer archive.</p> <p>Some agencies (e.g. SEPTA's <code>gtfs_public.zip</code>) distribute multiple GTFS feeds as individual zips inside a single outer zip. When configured, the adapter unwraps the outer archive and treats the selected inner zip as the static GTFS feed.</p>"},{"location":"reference/gtfs/#nibble.gtfs.static.load_static_gtfs","title":"<code>load_static_gtfs(url, auth=None, fill_shape_dist_traveled=True, inner_zip=None)</code>","text":"<p>Download and parse a static GTFS ZIP from a URL. Synchronous; runs at startup.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>URL of the static GTFS ZIP archive.</p> required <code>auth</code> <code>Auth | None</code> <p>Optional httpx auth strategy (e.g. query-param or header key).</p> <code>None</code> <code>fill_shape_dist_traveled</code> <code>bool</code> <p>When <code>True</code> (the default), back-fill <code>shape_dist_traveled</code> for stop times that lack it.  Pass <code>False</code> for feeds that already include complete values.</p> <code>True</code> <p>Returns:</p> Type Description <code>StaticGTFS</code> <p>A <code>StaticGTFS</code> object with populated trip and stop-time indexes.</p> <p>Raises:</p> Type Description <code>HTTPStatusError</code> <p>If the download returns a non-2xx response.</p>"},{"location":"reference/gtfs/#nibble.gtfs.static.load_static_gtfs_from_bytes","title":"<code>load_static_gtfs_from_bytes(content, fill_shape_dist_traveled=True)</code>","text":"<p>Parse a static GTFS ZIP from already-downloaded bytes.</p> <p>Use this when the ZIP was already fetched (e.g. by the fixer/publisher) to avoid downloading it a second time.</p> <p>Parameters:</p> Name Type Description Default <code>content</code> <code>bytes</code> <p>Raw bytes of a GTFS ZIP archive.</p> required <code>fill_shape_dist_traveled</code> <code>bool</code> <p>When <code>True</code> (the default), back-fill <code>shape_dist_traveled</code> for stop times that lack it.  Pass <code>False</code> for feeds that already include complete values.</p> <code>True</code> <p>Returns:</p> Type Description <code>StaticGTFS</code> <p>A <code>StaticGTFS</code> object with populated trip and stop-time indexes.</p>"},{"location":"reference/gtfs/#nibble.gtfs.static.infer_stop_from_position","title":"<code>infer_stop_from_position(lat, lon, trip_id, gtfs)</code>","text":"<p>Infer stop_id, stop_sequence, and current_status from a raw vehicle position.</p> <p>Projects the vehicle onto the trip's shape polyline using <code>shape_dist_traveled</code>, then determines the next stop ahead and whether the vehicle is stopped at it.</p> <p>Returns <code>(None, None, \"IN_TRANSIT_TO\")</code> when inference is not possible (trip not found, no shape, no stop times, or stop times lack <code>shape_dist_traveled</code>).</p> <p>Parameters:</p> Name Type Description Default <code>lat</code> <code>float</code> <p>Vehicle latitude in WGS84 decimal degrees.</p> required <code>lon</code> <code>float</code> <p>Vehicle longitude in WGS84 decimal degrees.</p> required <code>trip_id</code> <code>str</code> <p>GTFS trip identifier for the vehicle's current trip.</p> required <code>gtfs</code> <code>StaticGTFS</code> <p>Loaded static GTFS indexes.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>A <code>(stop_id, stop_sequence, current_status)</code> tuple.  <code>current_status</code></p> <code>int | None</code> <p>is <code>\"STOPPED_AT\"</code> when the vehicle is within</p> <code>Literal['INCOMING_AT', 'STOPPED_AT', 'IN_TRANSIT_TO']</code> <p>data:<code>_STOPPED_THRESHOLD_M</code> metres of a stop, otherwise</p> <code>tuple[str | None, int | None, Literal['INCOMING_AT', 'STOPPED_AT', 'IN_TRANSIT_TO']]</code> <p><code>\"IN_TRANSIT_TO\"</code>.</p>"},{"location":"reference/gtfs/#nibble.gtfs.static.infer_trip_from_position","title":"<code>infer_trip_from_position(lat, lon, route_id, gtfs, timestamp=None, agency_timezone=None, bearing=None)</code>","text":"<p>Infer the most likely <code>trip_id</code> for a vehicle given its position and route.</p> <p>Iterates over all trips for route_id that have an associated shape and computes the minimum perpendicular distance from (lat, lon) to each shape polyline.</p> <p>When both timestamp and agency_timezone are supplied, trips whose scheduled time window (first-stop to last-stop departure/arrival times, with a \u00b130-minute tolerance) does not bracket the current local time are excluded before the distance ranking.  When no trips survive the time filter the constraint is dropped and all candidates are ranked by distance alone, so the function always returns the best geometric match.</p> <p>Returns <code>None</code> only when route_id is not in the index or no candidate trip has a shape.</p> <p>Note: if two trips share the same shape (same physical path, different times of day) the returned <code>trip_id</code> is one of them arbitrarily, but stop inference via :func:<code>infer_stop_from_position</code> will still be correct since the shapes are identical.</p> <p>Parameters:</p> Name Type Description Default <code>lat</code> <code>float</code> <p>Vehicle latitude in WGS84 decimal degrees.</p> required <code>lon</code> <code>float</code> <p>Vehicle longitude in WGS84 decimal degrees.</p> required <code>route_id</code> <code>str</code> <p>GTFS route identifier reported by the feed.</p> required <code>gtfs</code> <code>StaticGTFS</code> <p>Loaded static GTFS indexes.</p> required <code>timestamp</code> <code>datetime | None</code> <p>UTC-aware datetime of the observation.  Required for time-of-day filtering.</p> <code>None</code> <code>agency_timezone</code> <code>str | None</code> <p>IANA timezone string (e.g. <code>\"America/New_York\"</code>). Required for time-of-day filtering.</p> <code>None</code> <code>bearing</code> <code>float | None</code> <p>Vehicle heading in degrees clockwise from north (0-359), or <code>None</code> if not reported.  When provided, trips whose shape runs more than :data:<code>_BEARING_TOLERANCE_DEG</code> degrees opposite to the vehicle's heading at the projected point are excluded.  The filter is skipped when no candidates survive it.</p> <code>None</code> <p>Returns:</p> Type Description <code>str | None</code> <p>The best-matching <code>trip_id</code>, or <code>None</code> if no match is possible.</p>"},{"location":"reference/gtfs/#realtime-fetcher","title":"Realtime fetcher","text":""},{"location":"reference/gtfs/#nibble.gtfs.realtime","title":"<code>nibble.gtfs.realtime</code>","text":"<p>Async GTFS-RT protobuf feed fetcher.</p>"},{"location":"reference/gtfs/#nibble.gtfs.realtime.fetch_feed","title":"<code>fetch_feed(url, client)</code>  <code>async</code>","text":"<p>Fetch and parse a GTFS-RT VehiclePositions protobuf feed.</p> <p>All errors (network failure, non-200 response, protobuf parse error) are caught and logged, returning <code>None</code> so the caller can skip the cycle without crashing.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>The GTFS-RT endpoint URL.</p> required <code>client</code> <code>AsyncClient</code> <p>Shared async HTTP client with a 30-second timeout.</p> required <p>Returns:</p> Type Description <code>FeedMessage | None</code> <p>A parsed <code>FeedMessage</code>, or <code>None</code> on any error.</p>"},{"location":"reference/gtfs/#feed-info-parser","title":"Feed info parser","text":""},{"location":"reference/gtfs/#nibble.gtfs.feed_info","title":"<code>nibble.gtfs.feed_info</code>","text":"<p>Parse feed_info.txt from a GTFS ZIP to extract feed metadata.</p>"},{"location":"reference/gtfs/#nibble.gtfs.feed_info.FeedInfo","title":"<code>FeedInfo</code>  <code>dataclass</code>","text":"<p>Metadata from a GTFS feed_info.txt file.</p> <p>Attributes:</p> Name Type Description <code>feed_start_date</code> <code>str</code> <p>Date the feed becomes valid, in <code>YYYYMMDD</code> format.</p> <code>feed_end_date</code> <code>str</code> <p>Date after which the feed is no longer valid, in <code>YYYYMMDD</code> format.</p> <code>feed_version</code> <code>str</code> <p>Publisher-assigned version string for this feed.</p>"},{"location":"reference/gtfs/#nibble.gtfs.feed_info.dates_from_calendar","title":"<code>dates_from_calendar(zf)</code>","text":"<p>Derive feed start/end dates from calendar.txt and calendar_dates.txt.</p> <p>Returns a <code>(start_date, end_date)</code> tuple in <code>YYYYMMDD</code> format.  Falls back to empty strings when neither file is present.</p>"},{"location":"reference/gtfs/#nibble.gtfs.feed_info.parse_feed_info","title":"<code>parse_feed_info(zip_content)</code>","text":"<p>Extract FeedInfo from a GTFS ZIP's feed_info.txt.</p> <p>Parameters:</p> Name Type Description Default <code>zip_content</code> <code>bytes</code> <p>Raw bytes of a GTFS ZIP archive.</p> required <p>Returns:</p> Type Description <code>FeedInfo | None</code> <p>A <code>FeedInfo</code> populated from the first row of <code>feed_info.txt</code>, or</p> <code>FeedInfo | None</code> <p><code>None</code> if the file is absent from the archive.</p>"},{"location":"reference/gtfs/#data-fixer","title":"Data fixer","text":""},{"location":"reference/gtfs/#nibble.gtfs.fixer","title":"<code>nibble.gtfs.fixer</code>","text":"<p>Fix common issues in GTFS static ZIP archives before loading or publishing.</p> <p>Each fixer is a callable that receives the raw text of a single CSV file and returns corrected text. Fixers are applied to every .txt file in the ZIP.</p>"},{"location":"reference/gtfs/#nibble.gtfs.fixer.fix_gtfs_zip","title":"<code>fix_gtfs_zip(content)</code>","text":"<p>Apply all registered fixers to every .txt file in a GTFS ZIP.</p> <p>Non-CSV files are passed through unchanged. The returned ZIP has the same file structure as the input but with corrected CSV content.</p> <p>Fixers applied (in order): BOM stripping, CRLF normalisation, field whitespace stripping.</p> <p>Parameters:</p> Name Type Description Default <code>content</code> <code>bytes</code> <p>Raw bytes of the original GTFS ZIP archive.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Bytes of a new ZIP archive with all <code>.txt</code> files corrected.</p>"},{"location":"reference/gtfs/#s3-publisher","title":"S3 publisher","text":""},{"location":"reference/gtfs/#nibble.gtfs.publisher","title":"<code>nibble.gtfs.publisher</code>","text":"<p>Upload a fixed GTFS ZIP to S3 and maintain archived_feeds.txt for gobble.</p> <p>The archived_feeds.txt format (CSV, no header quoting):     feed_start_date,feed_end_date,feed_version,archive_url,archive_note</p> <p>The most recent entry must appear first. Each call to publish_gtfs_to_s3() downloads the existing file (if any), prepends the new row, and re-uploads it.</p> <p>Requires boto3 (install nibble[s3]).</p>"},{"location":"reference/gtfs/#nibble.gtfs.publisher.publish_gtfs_to_s3","title":"<code>publish_gtfs_to_s3(zip_bytes, feed_info, bucket, prefix, archived_feeds_key, region='us-east-1', archive_url_base=None)</code>","text":"<p>Upload the fixed GTFS ZIP and update archived_feeds.txt on S3.</p> <p>Returns the archive_url written into archived_feeds.txt.</p> <p>Parameters:</p> Name Type Description Default <code>zip_bytes</code> <code>bytes</code> <p>Fixed GTFS ZIP content to upload.</p> required <code>feed_info</code> <code>FeedInfo</code> <p>Metadata extracted from <code>feed_info.txt</code> used to name the ZIP (<code>{prefix}/{feed_start_date}.zip</code>) and populate the index row.</p> required <code>bucket</code> <code>str</code> <p>S3 bucket name.</p> required <code>prefix</code> <code>str</code> <p>Key prefix for the ZIP (e.g. <code>\"gtfs\"</code>).</p> required <code>archived_feeds_key</code> <code>str</code> <p>Full S3 key for the <code>archived_feeds.txt</code> index.</p> required <code>region</code> <code>str</code> <p>AWS region used for the S3 client and to construct the archive URL.</p> <code>'us-east-1'</code> <code>archive_url_base</code> <code>str | None</code> <p>When set, the archive_url stored in the index is <code>{archive_url_base}/{feed_start_date}.zip</code> (e.g. <code>\"/gtfs\"</code>). When <code>None</code>, the public S3 HTTPS URL is used instead.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>The archive_url written into archived_feeds.txt.</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>If <code>boto3</code> is not installed (install <code>nibble[s3]</code>).</p>"},{"location":"reference/gtfs/#nibble.gtfs.publisher.fetch_fixed_bundle_from_s3","title":"<code>fetch_fixed_bundle_from_s3(feed_start_date, bucket, prefix, region='us-east-1')</code>","text":"<p>Download a previously-fixed GTFS ZIP from S3, or return None if absent.</p> <p>Parameters:</p> Name Type Description Default <code>feed_start_date</code> <code>str</code> <p>Feed start date in <code>YYYYMMDD</code> format, used as the ZIP key (<code>{prefix}/{feed_start_date}.zip</code>).</p> required <code>bucket</code> <code>str</code> <p>S3 bucket name.</p> required <code>prefix</code> <code>str</code> <p>Key prefix for the ZIP (e.g. <code>\"gtfs\"</code>).</p> required <code>region</code> <code>str</code> <p>AWS region used for the S3 client.</p> <code>'us-east-1'</code> <p>Returns:</p> Type Description <code>bytes | None</code> <p>ZIP bytes if the object exists, or <code>None</code> if it is not found.</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>If <code>boto3</code> is not installed (install <code>nibble[s3]</code>).</p>"},{"location":"reference/interpolator/","title":"Interpolator","text":"<p>Schedule-aware stop-gap interpolation between polling intervals.</p>"},{"location":"reference/interpolator/#nibble.interpolator","title":"<code>nibble.interpolator</code>","text":"<p>Schedule-aware stop-gap interpolation between polling intervals.</p>"},{"location":"reference/interpolator/#nibble.interpolator.interpolate","title":"<code>interpolate(prev, curr, gtfs, max_stops)</code>","text":"<p>Produce synthetic VehicleEvent instances for stops between prev and curr.</p> <p>Uses scheduled departure/arrival times from static GTFS to distribute timestamps proportionally across the gap. Falls back to linear interpolation when stop time data is unavailable.</p> <p>Parameters:</p> Name Type Description Default <code>prev</code> <code>VehicleState</code> <p>Last known vehicle state from the state store (provides previous stop sequence and timestamp).</p> required <code>curr</code> <code>VehicleEvent</code> <p>Current observed <code>VehicleEvent</code> (provides current stop sequence, position, and trip context).</p> required <code>gtfs</code> <code>StaticGTFS</code> <p>Static GTFS indexes used to look up scheduled stop times.</p> required <code>max_stops</code> <code>int</code> <p>Maximum gap size to interpolate. Gaps larger than this are returned as an empty list.</p> required <p>Returns:</p> Type Description <code>list[VehicleEvent]</code> <p>A list of <code>VehicleEvent</code> objects for each stop in the gap, ending with</p> <code>list[VehicleEvent]</code> <p><code>curr</code> (tagged <code>provenance=\"observed\"</code>). Intermediate stops are tagged</p> <code>list[VehicleEvent]</code> <p><code>provenance=\"interpolated\"</code>. Returns an empty list if interpolation is</p> <code>list[VehicleEvent]</code> <p>not possible or not warranted.</p>"},{"location":"reference/models/","title":"Models","text":"<p>Core immutable data structures that flow through the nibble pipeline.</p>"},{"location":"reference/models/#nibble.models","title":"<code>nibble.models</code>","text":"<p>Core immutable data structures flowing through the nibble pipeline.</p>"},{"location":"reference/models/#nibble.models.Position","title":"<code>Position</code>  <code>dataclass</code>","text":"<p>GPS position reported by a vehicle.</p> <p>latitude/longitude are WGS84 decimal degrees. bearing is degrees clockwise from north (0-359). speed is meters per second. Both bearing and speed may be absent from the feed.</p> <p>Attributes:</p> Name Type Description <code>latitude</code> <code>float</code> <p>WGS84 decimal degrees.</p> <code>longitude</code> <code>float</code> <p>WGS84 decimal degrees.</p> <code>bearing</code> <code>float | None</code> <p>Heading in degrees clockwise from north (0-359), or <code>None</code> if the feed does not report it.</p> <code>speed</code> <code>float | None</code> <p>Speed in meters per second, or <code>None</code> if not reported.</p>"},{"location":"reference/models/#nibble.models.VehicleEvent","title":"<code>VehicleEvent</code>  <code>dataclass</code>","text":"<p>Immutable snapshot of a vehicle's state at a point in time.</p> <p>Every event carries two quality tags:</p> <p>provenance - how the event was produced:</p> <ul> <li><code>\"observed\"</code> - directly reported by the GTFS-RT feed</li> <li><code>\"inferred\"</code> - position observed, but trip info carried forward from a prior poll</li> <li><code>\"interpolated\"</code> - synthetic event generated to fill a stop gap between polls</li> <li><code>\"manual\"</code> - trip assignment issued by an operator via the corrections API</li> </ul> <p>confidence - certainty about the vehicle's trip assignment:</p> <ul> <li><code>\"confirmed\"</code> - vehicle reported a trip_id in this poll</li> <li><code>\"inferred\"</code> - trip_id carried forward; vehicle may have changed trips</li> <li><code>\"stale\"</code> - vehicle exceeded the stale threshold; a remove event follows</li> </ul> <p>Attributes:</p> Name Type Description <code>vehicle_id</code> <code>str</code> <p>Stable identifier for the vehicle (from the GTFS-RT feed).</p> <code>position</code> <code>Position</code> <p>Current GPS position.</p> <code>timestamp</code> <code>datetime</code> <p>UTC timestamp of this observation.</p> <code>provenance</code> <code>Literal['observed', 'inferred', 'interpolated', 'manual']</code> <p>How this event was produced (<code>\"observed\"</code>, <code>\"inferred\"</code>, <code>\"interpolated\"</code>, or <code>\"manual\"</code>).</p> <code>confidence</code> <code>Literal['confirmed', 'inferred', 'stale']</code> <p>Certainty about the trip assignment (<code>\"confirmed\"</code>, <code>\"inferred\"</code>, or <code>\"stale\"</code>).</p> <code>trip_id</code> <code>str | None</code> <p>GTFS trip identifier, or <code>None</code> if unknown.</p> <code>route_id</code> <code>str | None</code> <p>GTFS route identifier, or <code>None</code> if unknown.</p> <code>stop_id</code> <code>str | None</code> <p>Current stop identifier, or <code>None</code> if unknown.</p> <code>current_stop_sequence</code> <code>int | None</code> <p>Stop sequence number within the current trip, or <code>None</code> if unknown.</p> <code>current_status</code> <code>Literal['INCOMING_AT', 'STOPPED_AT', 'IN_TRANSIT_TO']</code> <p>Vehicle status relative to the current stop - <code>\"INCOMING_AT\"</code>, <code>\"STOPPED_AT\"</code>, or <code>\"IN_TRANSIT_TO\"</code>.</p> <code>direction_id</code> <code>int | None</code> <p>GTFS direction (<code>0</code> or <code>1</code>), or <code>None</code> if unknown.</p> <code>label</code> <code>str | None</code> <p>Human-readable vehicle label (e.g. bus number), or <code>None</code>.</p>"},{"location":"reference/models/#nibble.models.SSEEvent","title":"<code>SSEEvent</code>  <code>dataclass</code>","text":"<p>An SSE envelope ready for broadcast to clients.</p> <p>Attributes:</p> Name Type Description <code>event_type</code> <code>Literal['reset', 'add', 'update', 'remove']</code> <p>The SSE event name - <code>\"reset\"</code>, <code>\"add\"</code>, <code>\"update\"</code>, or <code>\"remove\"</code>.</p> <ul> <li><code>\"reset\"</code> - full snapshot of all known vehicles; sent once to new subscribers;   <code>data</code> is a list of serialized vehicle dicts (JSON:API format).</li> <li><code>\"add\"</code> - a vehicle has appeared in the feed for the first time since the last   reset; <code>data</code> is a single serialized vehicle dict (JSON:API format).</li> <li><code>\"update\"</code> - a vehicle already in the feed changed state; <code>data</code> is a single   serialized vehicle dict (JSON:API format).</li> <li><code>\"remove\"</code> - a single vehicle has left the feed; <code>data</code> is <code>{\"id\": ...}</code>.</li> </ul> <code>data</code> <code>list[dict[str, Any]] | dict[str, Any]</code> <p>A single serialized vehicle dict for <code>\"add\"</code>/<code>\"update\"</code>/<code>\"remove\"</code> events, or a list of vehicle dicts for <code>\"reset\"</code> events.</p>"},{"location":"reference/models/#nibble.models.Trip","title":"<code>Trip</code>  <code>dataclass</code>","text":"<p>Static GTFS trip metadata, loaded from trips.txt.</p> <p>Attributes:</p> Name Type Description <code>trip_id</code> <code>str</code> <p>Unique GTFS trip identifier.</p> <code>route_id</code> <code>str</code> <p>Route this trip belongs to.</p> <code>direction_id</code> <code>int | None</code> <p>Travel direction (<code>0</code> or <code>1</code>), or <code>None</code> if absent.</p> <code>shape_id</code> <code>str | None</code> <p>Associated shape identifier, or <code>None</code> if absent.</p>"},{"location":"reference/models/#nibble.models.StopTime","title":"<code>StopTime</code>  <code>dataclass</code>","text":"<p>Scheduled stop entry from stop_times.txt.</p> <p><code>arrival_time</code> and <code>departure_time</code> are GTFS HH:MM:SS strings. Hours may exceed 23 for service running past midnight (e.g. <code>\"25:30:00\"</code>).</p> <p>Attributes:</p> Name Type Description <code>trip_id</code> <code>str</code> <p>Trip this stop time belongs to.</p> <code>stop_id</code> <code>str</code> <p>Stop being served.</p> <code>stop_sequence</code> <code>int</code> <p>Ordering of the stop within the trip.</p> <code>arrival_time</code> <code>str | None</code> <p>Scheduled arrival as a GTFS HH:MM:SS string, or <code>None</code>.</p> <code>departure_time</code> <code>str | None</code> <p>Scheduled departure as a GTFS HH:MM:SS string, or <code>None</code>.</p> <code>shape_dist_traveled</code> <code>float | None</code> <p>Distance along the shape from the first stop to this stop, in the units used by the feed's <code>shapes.txt</code>, or <code>None</code> if not provided.</p>"},{"location":"reference/normalizers/","title":"Normalizers","text":"<p>Agency-specific feed normalizers that fix quirks before parsing.</p>"},{"location":"reference/normalizers/#base-class","title":"Base class","text":""},{"location":"reference/normalizers/#nibble.normalizer.base","title":"<code>nibble.normalizer.base</code>","text":"<p>Abstract base class for agency-specific feed normalizers.</p>"},{"location":"reference/normalizers/#nibble.normalizer.base.BaseNormalizer","title":"<code>BaseNormalizer</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Contract for agency-specific feed normalizers.</p> <p>A normalizer receives the raw GTFS-RT <code>FeedMessage</code> and the loaded <code>StaticGTFS</code>, may mutate the feed to fix agency quirks, and returns the (possibly modified) <code>FeedMessage</code>. It runs before <code>_parse_feed</code> so downstream code never sees the raw agency data.</p>"},{"location":"reference/normalizers/#nibble.normalizer.base.BaseNormalizer.normalize","title":"<code>normalize(feed, gtfs)</code>  <code>abstractmethod</code>","text":"<p>Normalize the feed (potentially in place) and return it.</p> <p>Parameters:</p> Name Type Description Default <code>feed</code> <code>FeedMessage</code> <p>The raw <code>FeedMessage</code> from the adapter.</p> required <code>gtfs</code> <code>StaticGTFS</code> <p>Static GTFS indexes, available for cross-referencing trip IDs.</p> required <p>Returns:</p> Type Description <code>FeedMessage</code> <p>The (possibly mutated) <code>FeedMessage</code> ready for parsing.</p>"},{"location":"reference/normalizers/#default-pass-through","title":"Default (pass-through)","text":""},{"location":"reference/normalizers/#nibble.normalizer.default","title":"<code>nibble.normalizer.default</code>","text":"<p>Pass-through normalizer for well-behaved feeds.</p>"},{"location":"reference/normalizers/#nibble.normalizer.default.DefaultNormalizer","title":"<code>DefaultNormalizer</code>","text":"<p>               Bases: <code>BaseNormalizer</code></p> <p>No-op normalizer. Returns the feed unmodified.</p>"},{"location":"reference/normalizers/#nibble.normalizer.default.DefaultNormalizer.normalize","title":"<code>normalize(feed, gtfs)</code>","text":"<p>Return the feed unchanged.</p> <p>Parameters:</p> Name Type Description Default <code>feed</code> <code>FeedMessage</code> <p>The raw <code>FeedMessage</code> from the adapter.</p> required <code>gtfs</code> <code>StaticGTFS</code> <p>Static GTFS indexes (unused).</p> required <p>Returns:</p> Type Description <code>FeedMessage</code> <p>The unmodified <code>feed</code>.</p>"},{"location":"reference/normalizers/#brta","title":"BRTA","text":""},{"location":"reference/normalizers/#nibble.normalizer.brta","title":"<code>nibble.normalizer.brta</code>","text":"<p>Normalizer for BRTA (Berkshire Regional Transit Authority) feeds.</p>"},{"location":"reference/normalizers/#nibble.normalizer.brta.BrtaNormalizer","title":"<code>BrtaNormalizer</code>","text":"<p>               Bases: <code>BaseNormalizer</code></p> <p>Normalizer for BRTA (Berkshire Regional Transit Authority) feeds.</p> <p>The RouteMatch API reports <code>masterRouteId</code> values like <code>\"Wk Rt 01\"</code> and <code>tripId</code> values like <code>\"Rte 01 1130 in\"</code> that do not match BRTA's static GTFS. This normalizer:</p> <ol> <li>Remaps route_id by extracting the numeric short name and looking it up    in the <code>route_short_names</code> index built from <code>routes.txt</code>.</li> <li>Clears trip_id entirely, since RouteMatch trip IDs have no relationship    to GTFS trip IDs. This lets nibble's position-based trip inference run    rather than failing to find the trip silently.</li> </ol>"},{"location":"reference/normalizers/#ccrta","title":"Ccrta","text":""},{"location":"reference/normalizers/#nibble.normalizer.ccrta","title":"<code>nibble.normalizer.ccrta</code>","text":"<p>Normalizer for CCRTA (Cape Cod Regional Transit Authority) feeds.</p>"},{"location":"reference/normalizers/#nibble.normalizer.ccrta.CcrtaNormalizer","title":"<code>CcrtaNormalizer</code>","text":"<p>               Bases: <code>BaseNormalizer</code></p> <p>Normalizer for CCRTA feeds.</p> <p>CCRTA's static GTFS uses internal numeric route_ids (e.g. <code>\"2976\"</code>) with descriptive long names (e.g. <code>\"Sealine Hyannis-Falmouth/Woods Hole\"</code>), but the RT feed reports abbreviated route names (e.g. <code>\"Sealine\"</code>).  This normalizer remaps abbreviated names to static GTFS route_ids using case-insensitive prefix and substring matching against <code>route_long_name</code>.</p>"},{"location":"reference/normalizers/#nibble.normalizer.ccrta.CcrtaNormalizer.normalize","title":"<code>normalize(feed, gtfs)</code>","text":"<p>Remap CCRTA abbreviated route names to their static GTFS route_ids.</p> <p>Parameters:</p> Name Type Description Default <code>feed</code> <code>FeedMessage</code> <p>The raw <code>FeedMessage</code> from the CCRTA feed.</p> required <code>gtfs</code> <code>StaticGTFS</code> <p>Static GTFS indexes containing the <code>route_short_names</code> map (which for CCRTA holds long name \u2192 route_id entries).</p> required <p>Returns:</p> Type Description <code>FeedMessage</code> <p>The modified <code>FeedMessage</code> with normalised route IDs.</p>"},{"location":"reference/normalizers/#cttransit","title":"Cttransit","text":""},{"location":"reference/normalizers/#nibble.normalizer.cttransit","title":"<code>nibble.normalizer.cttransit</code>","text":"<p>Normalizer for CTTransit (Connecticut Transit) feeds.</p>"},{"location":"reference/normalizers/#nibble.normalizer.cttransit.CttransitNormalizer","title":"<code>CttransitNormalizer</code>","text":"<p>               Bases: <code>BaseNormalizer</code></p> <p>Normalizer for CTTransit GTFS-RT feeds.</p> <p>CTTransit's feed populates <code>trip_id</code> but omits <code>route_id</code>. This normalizer fills in <code>route_id</code> from the static GTFS <code>trips</code> index.</p>"},{"location":"reference/normalizers/#mta-railroad","title":"Mta Railroad","text":""},{"location":"reference/normalizers/#nibble.normalizer.mta_railroad","title":"<code>nibble.normalizer.mta_railroad</code>","text":"<p>Normalizer for MTA Railroad feeds (Metro-North and LIRR).</p>"},{"location":"reference/normalizers/#nibble.normalizer.mta_railroad.MtaRailroadNormalizer","title":"<code>MtaRailroadNormalizer</code>","text":"<p>               Bases: <code>BaseNormalizer</code></p> <p>Normalizer for MTA Railroad GTFS-RT feeds (Metro-North and LIRR).</p> <p>Both MNR and LIRR feeds report train numbers as trip IDs (e.g. <code>\"1300\"</code>) while their static GTFS files use composite or opaque trip IDs. The train number corresponds to the <code>trip_short_name</code> field in <code>trips.txt</code>.</p> <p>This normalizer rewrites each RT train-number trip ID to its full static trip ID via the <code>trip_short_names</code> index built by the static GTFS loader, so that downstream stop-time and interpolation lookups succeed.</p>"},{"location":"reference/normalizers/#nibble.normalizer.mta_railroad.MtaRailroadNormalizer.normalize","title":"<code>normalize(feed, gtfs)</code>","text":"<p>Rewrite MTA Railroad train numbers to their full static GTFS trip IDs.</p> <p>Parameters:</p> Name Type Description Default <code>feed</code> <code>FeedMessage</code> <p>The raw <code>FeedMessage</code> from the adapter.</p> required <code>gtfs</code> <code>StaticGTFS</code> <p>Static GTFS indexes, including the <code>trip_short_names</code> mapping populated from <code>trips.txt</code>.</p> required <p>Returns:</p> Type Description <code>FeedMessage</code> <p>The modified <code>FeedMessage</code> with train-number trip IDs replaced</p> <code>FeedMessage</code> <p>by their corresponding static trip IDs where a match exists.</p> <code>FeedMessage</code> <p>Unmatched IDs are left unchanged.</p>"},{"location":"reference/normalizers/#mwrta","title":"MWRTA","text":""},{"location":"reference/normalizers/#nibble.normalizer.mwrta","title":"<code>nibble.normalizer.mwrta</code>","text":"<p>Normalizer for MWRTA (MetroWest Regional Transit Authority) feeds.</p>"},{"location":"reference/normalizers/#nibble.normalizer.mwrta.MwrtaNormalizer","title":"<code>MwrtaNormalizer</code>","text":"<p>               Bases: <code>BaseNormalizer</code></p> <p>Normalizer for MWRTA feeds.</p> <p>MWRTA's static GTFS uses internal UUIDs for route_id, but the GTFS-RT feed reports human-readable short names (e.g. <code>\"06\"</code>, <code>\"07\"</code>).  This normalizer remaps the feed's route_id to the corresponding UUID using the <code>route_short_names</code> index built from <code>routes.txt</code>.</p>"},{"location":"reference/normalizers/#nibble.normalizer.mwrta.MwrtaNormalizer.normalize","title":"<code>normalize(feed, gtfs)</code>","text":"<p>Remap MWRTA route short names to their static GTFS UUID route_ids.</p> <p>Parameters:</p> Name Type Description Default <code>feed</code> <code>FeedMessage</code> <p>The raw <code>FeedMessage</code> from the MWRTA feed.</p> required <code>gtfs</code> <code>StaticGTFS</code> <p>Static GTFS indexes containing the <code>route_short_names</code> map.</p> required <p>Returns:</p> Type Description <code>FeedMessage</code> <p>The modified <code>FeedMessage</code> with normalised route IDs.</p>"},{"location":"reference/normalizers/#nyct","title":"Nyct","text":""},{"location":"reference/normalizers/#nibble.normalizer.nyct","title":"<code>nibble.normalizer.nyct</code>","text":"<p>Normalizer for MTA NYCT subway feeds.</p>"},{"location":"reference/normalizers/#nibble.normalizer.nyct.NyctNormalizer","title":"<code>NyctNormalizer</code>","text":"<p>               Bases: <code>BaseNormalizer</code></p> <p>Normalizer for MTA NYCT subway GTFS-RT feeds.</p> <p>The NYCT static GTFS uses full trip IDs that include a service-period prefix (e.g. <code>\"BFA25GEN-B087-Weekday-00_067600_B..S65R\"</code>), while the GTFS-RT feed publishes only the suffix (e.g. <code>\"067600_B..S65R\"</code>).</p> <p>On first use with a given static GTFS, this normalizer builds a suffix index mapping each short trip ID to its full static counterpart. Each incoming RT trip ID that matches a suffix is rewritten to the full form so that downstream trip lookups (stop times, interpolation) succeed.</p> <p>Trip IDs with no suffix match (e.g. B-train express variants not present in the static GTFS) are left unchanged; the state machine handles them via its unknown-trip-id path.</p> <p>NYCT extension fields processed per entity:</p> <ul> <li><code>is_assigned</code>: Entities with <code>is_assigned=False</code> are dropped \u2014 they   are phantom scheduled trips with no physical train assigned and produce   spurious unknown-trip-id warnings downstream.</li> <li><code>train_id</code>: Physical train/consist identifier from ATS. Written to   <code>vehicle.vehicle.id</code> when the feed leaves that field empty.</li> <li><code>direction</code>: Cardinal direction (NORTH/SOUTH) mapped to GTFS   <code>direction_id</code> (0/1) when the trip descriptor does not already carry one.</li> </ul>"},{"location":"reference/normalizers/#nibble.normalizer.nyct.NyctNormalizer.normalize","title":"<code>normalize(feed, gtfs)</code>","text":"<p>Rewrite short NYCT trip IDs to their full static GTFS form, drop unassigned trips, and back-fill direction_id and vehicle ID from NYCT-specific protobuf extensions.</p> <p>Parameters:</p> Name Type Description Default <code>feed</code> <code>FeedMessage</code> <p>The raw <code>FeedMessage</code> from the NYCT GTFS-RT feed.</p> required <code>gtfs</code> <code>StaticGTFS</code> <p>Static GTFS indexes used to build the suffix lookup.</p> required <p>Returns:</p> Type Description <code>FeedMessage</code> <p>The modified <code>FeedMessage</code> with:</p> <code>FeedMessage</code> <ul> <li>Entities with <code>is_assigned=False</code> removed.</li> </ul> <code>FeedMessage</code> <ul> <li>Trip IDs expanded to full form where a static match exists.</li> </ul> <code>FeedMessage</code> <ul> <li><code>direction_id</code> set from the NYCT cardinal direction when absent.</li> </ul> <code>FeedMessage</code> <ul> <li><code>vehicle.id</code> populated from <code>train_id</code> when absent.</li> </ul>"},{"location":"reference/normalizers/#passio-go","title":"Passio GO!","text":""},{"location":"reference/normalizers/#nibble.normalizer.passio","title":"<code>nibble.normalizer.passio</code>","text":"<p>Normalizer for Passio GO! feeds.</p> <p>After the PassioAdapter maps <code>routeId</code> \u2192 <code>shortName</code> (or <code>name</code>) via the routes endpoint, this normalizer resolves any values that don't directly match a GTFS <code>route_id</code> by looking them up in the <code>route_short_names</code> index (short_name \u2192 route_id).  Values that match neither are logged and recorded in <code>unknown_routes</code>.</p>"},{"location":"reference/normalizers/#nibble.normalizer.passio.PassioNormalizer","title":"<code>PassioNormalizer</code>","text":"<p>               Bases: <code>BaseNormalizer</code></p> <p>Resolve Passio route short names / display names to GTFS route_ids.</p>"},{"location":"reference/normalizers/#ripta","title":"RIPTA","text":""},{"location":"reference/normalizers/#nibble.normalizer.ripta","title":"<code>nibble.normalizer.ripta</code>","text":"<p>Normalizer for RIPTA (Rhode Island Public Transit Authority) feeds.</p>"},{"location":"reference/normalizers/#nibble.normalizer.ripta.RiptaNormalizer","title":"<code>RiptaNormalizer</code>","text":"<p>               Bases: <code>BaseNormalizer</code></p> <p>Normalizer for RIPTA (Rhode Island Public Transit Authority) feeds.</p> <p>RIPTA feeds may include trip_ids that don't directly match trips.txt. This normalizer attempts to reconcile known formatting quirks.</p>"},{"location":"reference/normalizers/#nibble.normalizer.ripta.RiptaNormalizer.normalize","title":"<code>normalize(feed, gtfs)</code>","text":"<p>Strip date suffixes from RIPTA trip_ids that don't match static GTFS.</p> <p>RIPTA sometimes appends a date component to trip IDs (e.g. <code>\"trip-123_20240101\"</code>). This method strips the suffix and remaps the entity to the base trip ID when the base is found in static GTFS.</p> <p>Parameters:</p> Name Type Description Default <code>feed</code> <code>FeedMessage</code> <p>The raw <code>FeedMessage</code> from the RIPTA feed.</p> required <code>gtfs</code> <code>StaticGTFS</code> <p>Static GTFS indexes used to check whether a trip ID is valid.</p> required <p>Returns:</p> Type Description <code>FeedMessage</code> <p>The modified <code>FeedMessage</code> with normalised trip IDs.</p>"},{"location":"reference/normalizers/#swiv","title":"Swiv","text":""},{"location":"reference/normalizers/#nibble.normalizer.swiv","title":"<code>nibble.normalizer.swiv</code>","text":"<p>Generic normalizer for Swiv-based feeds.</p> <p>After the SwivAdapter maps <code>idLigne</code> \u2192 <code>nomCommercial</code> via the topo endpoint, this normalizer resolves any <code>nomCommercial</code> values that don't directly match a GTFS <code>route_id</code> by looking them up in the <code>route_short_names</code> index (short_name \u2192 route_id).  Values that match neither are logged and recorded in <code>unknown_routes</code>.</p>"},{"location":"reference/normalizers/#nibble.normalizer.swiv.SwivNormalizer","title":"<code>SwivNormalizer</code>","text":"<p>               Bases: <code>BaseNormalizer</code></p> <p>Resolve Swiv nomCommercial values to GTFS route_ids.</p>"},{"location":"reference/normalizers/#vta","title":"VTA","text":""},{"location":"reference/normalizers/#nibble.normalizer.vta","title":"<code>nibble.normalizer.vta</code>","text":"<p>Normalizer for VTA (Vineyard Transit Authority) feeds.</p>"},{"location":"reference/normalizers/#nibble.normalizer.vta.VtaNormalizer","title":"<code>VtaNormalizer</code>","text":"<p>               Bases: <code>BaseNormalizer</code></p> <p>Normalizer for VTA (Vineyard Transit Authority) feeds.</p> <p>The MyTransitRide API reports <code>headsignText</code> values (e.g. <code>\"3\"</code>, <code>\"10\"</code>) as route identifiers, which match GTFS <code>route_short_name</code> but not the internal numeric <code>route_id</code> (e.g. <code>\"2801\"</code>, <code>\"2808\"</code>). This normalizer remaps them using the <code>route_short_names</code> index built from <code>routes.txt</code>.</p>"},{"location":"reference/normalizers/#nibble.normalizer.vta.VtaNormalizer.normalize","title":"<code>normalize(feed, gtfs)</code>","text":"<p>Remap VTA <code>headsignText</code> route identifiers to internal GTFS route IDs.</p> <p>The MyTransitRide API sets <code>trip.route_id</code> to a <code>route_short_name</code> value (e.g. <code>\"3\"</code>, <code>\"10\"</code>). This method replaces those values with the corresponding internal <code>route_id</code> from <code>routes.txt</code>, logging a warning and recording any unmatched identifiers in <code>unknown_routes</code>.</p> <p>Parameters:</p> Name Type Description Default <code>feed</code> <code>FeedMessage</code> <p>The raw FeedMessage from the VTA adapter.</p> required <code>gtfs</code> <code>StaticGTFS</code> <p>Loaded static GTFS providing the <code>route_short_names</code> index.</p> required <p>Returns:</p> Type Description <code>FeedMessage</code> <p>The same FeedMessage with <code>route_id</code> fields remapped in place.</p>"},{"location":"reference/normalizers/#wrta","title":"Wrta","text":""},{"location":"reference/normalizers/#nibble.normalizer.wrta","title":"<code>nibble.normalizer.wrta</code>","text":"<p>Normalizer for WRTA (Worcester Regional Transit Authority) feeds.</p> <p>WRTA uses a Swiv-based real-time feed.  When <code>NIBBLE_SWIV_TOPO_URL</code> is set, the SwivAdapter already replaces <code>idLigne</code> integers with <code>nomCommercial</code> route names before this normalizer runs, so the feed arrives with human-readable route IDs that match the GTFS directly.</p> <p>This normalizer acts as a fallback for any <code>idLigne</code> values the topo endpoint didn't cover, using a static map built from known Swiv line IDs.</p>"},{"location":"reference/normalizers/#nibble.normalizer.wrta.WrtaNormalizer","title":"<code>WrtaNormalizer</code>","text":"<p>               Bases: <code>SwivNormalizer</code></p> <p>Normalizer for WRTA: applies the hard-coded idLigne fallback map first, then delegates to SwivNormalizer for route_short_names lookup.</p>"},{"location":"reference/poller/","title":"Poller","text":"<p>Async polling loop that fetches, normalizes, parses, and reconciles feeds.</p>"},{"location":"reference/poller/#nibble.poller","title":"<code>nibble.poller</code>","text":"<p>Async polling loop: fetch \u2192 normalize \u2192 parse \u2192 reconcile \u2192 broadcast.</p>"},{"location":"reference/poller/#nibble.poller.poll_loop","title":"<code>poll_loop(config, gtfs, broadcaster, adapter=None, overrides=None, on_snapshot=None)</code>  <code>async</code>","text":"<p>Run the feed poll loop forever, broadcasting SSE events on each cycle.</p> <p>Handles transient errors (network failures, bad responses, parse errors) gracefully - a failed poll is logged and skipped; the loop continues on the next interval. Unexpected exceptions are also caught and logged so the loop never crashes the server.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>Settings</code> <p>Application settings providing the poll interval, stale threshold, normalizer name, and interpolation limits.</p> required <code>gtfs</code> <code>StaticGTFS | GtfsHolder</code> <p>Static GTFS indexes, or a <code>GtfsHolder</code> whose <code>.gtfs</code> attribute is read on every poll so live reloads are picked up.</p> required <code>broadcaster</code> <code>Broadcaster</code> <p>The pub/sub hub to push <code>SSEEvent</code> objects to after each successful poll.</p> required <code>adapter</code> <code>BaseAdapter | None</code> <p>Feed adapter to use. If <code>None</code>, a <code>GtfsRtAdapter</code> is created from <code>config.gtfs_rt_url</code> for backward compatibility.</p> <code>None</code> <code>overrides</code> <code>OverrideStore | None</code> <p>Optional store of operator-issued manual trip assignments. When provided, the state machine applies these before its normal resolution ladder.</p> <code>None</code> <code>on_snapshot</code> <code>Callable[[dict[str, VehicleEvent]], Awaitable[None]] | None</code> <p>Optional async callback invoked after each successful poll with the current vehicle snapshot. Errors are caught and logged so they never abort the poll loop.</p> <code>None</code>"},{"location":"reference/reconciler/","title":"Reconciler","text":"<p>Diff engine that compares vehicle snapshots and emits SSE events.</p>"},{"location":"reference/reconciler/#nibble.reconciler","title":"<code>nibble.reconciler</code>","text":"<p>Diff engine: compares vehicle snapshots and emits SSE events.</p>"},{"location":"reference/reconciler/#nibble.reconciler.reconcile","title":"<code>reconcile(prev, curr, state_store, gtfs, config)</code>","text":"<p>Diff prev and curr snapshots and return SSE events and the resolved snapshot.</p> <p>On the first call (empty <code>prev</code>), emits a single <code>\"reset\"</code> event containing all current vehicles. Subsequent calls emit <code>\"update\"</code> for new or changed vehicles and <code>\"remove\"</code> for vehicles that have disappeared or gone stale.</p> <p>Parameters:</p> Name Type Description Default <code>prev</code> <code>dict[str, VehicleEvent]</code> <p>Vehicle snapshot from the previous poll cycle (empty dict on first call).</p> required <code>curr</code> <code>dict[str, VehicleEvent]</code> <p>Vehicle snapshot from the current poll cycle, keyed by vehicle ID.</p> required <code>state_store</code> <code>StateStore</code> <p>Mutable state machine applied to each current vehicle to resolve confidence and carry forward trip information.</p> required <code>gtfs</code> <code>StaticGTFS</code> <p>Static GTFS indexes used by the state machine for trip validation.</p> required <code>config</code> <code>Settings</code> <p>Application settings (stale threshold, interpolation limits).</p> required <p>Returns:</p> Type Description <code>list[SSEEvent]</code> <p>A tuple of (sse_events, resolved_snapshot) where <code>sse_events</code> is a list of</p> <code>dict[str, VehicleEvent]</code> <p><code>SSEEvent</code> objects ready to broadcast and <code>resolved_snapshot</code> is the current</p> <code>tuple[list[SSEEvent], dict[str, VehicleEvent]]</code> <p>vehicle state after state machine resolution (with trip/stop inference applied).</p> <code>tuple[list[SSEEvent], dict[str, VehicleEvent]]</code> <p><code>sse_events</code> may be empty if nothing changed and no stale vehicles were detected.</p>"},{"location":"reference/server/","title":"Server","text":"<p>Starlette ASGI server: SSE endpoint, health check, and startup orchestration.</p>"},{"location":"reference/server/#nibble.server","title":"<code>nibble.server</code>","text":"<p>FastAPI ASGI server: SSE endpoint, health check, and startup orchestration.</p>"},{"location":"reference/server/#nibble.server.GtfsHolder","title":"<code>GtfsHolder</code>","text":"<p>Container for the current <code>StaticGTFS</code> instance, safe for asyncio use.</p> <p>The reload loop swaps :attr:<code>gtfs</code> in place; the poll loop always reads the latest value without needing a lock.  This is safe because both loops run in the same asyncio event loop \u2014 there are no <code>await</code> points between reading and using <code>holder.gtfs</code>, so no other coroutine can interleave.</p>"},{"location":"reference/server/#nibble.server.JsonFormatter","title":"<code>JsonFormatter</code>","text":"<p>               Bases: <code>Formatter</code></p> <p>Serialize log records to JSON lines for structured log aggregation.</p>"},{"location":"reference/server/#nibble.server.LoggingMiddleware","title":"<code>LoggingMiddleware</code>","text":"<p>ASGI middleware that logs each HTTP request with method, path, status, and duration.</p>"},{"location":"reference/server/#nibble.server.Broadcaster","title":"<code>Broadcaster</code>","text":"<p>Async pub/sub hub for SSE clients.</p> <p>Maintains a current snapshot of vehicle state so that new subscribers receive an immediate <code>reset</code> event without waiting for the next poll. Each subscriber gets its own <code>asyncio.Queue</code>; the poll loop pushes <code>SSEEvent</code> objects into every queue via <code>broadcast()</code>.</p>"},{"location":"reference/server/#nibble.server.Broadcaster.client_count","title":"<code>client_count</code>  <code>property</code>","text":"<p>Number of currently connected SSE clients.</p>"},{"location":"reference/server/#nibble.server.Broadcaster.subscribe","title":"<code>subscribe()</code>","text":"<p>Register a new SSE client and return its dedicated event queue.</p> <p>Returns:</p> Type Description <code>Queue[SSEEvent | None]</code> <p>A new <code>asyncio.Queue</code> that will receive <code>SSEEvent</code> objects as</p> <code>Queue[SSEEvent | None]</code> <p>they are broadcast. A <code>None</code> sentinel is enqueued to signal</p> <code>Queue[SSEEvent | None]</code> <p>stream termination.</p>"},{"location":"reference/server/#nibble.server.Broadcaster.unsubscribe","title":"<code>unsubscribe(q)</code>","text":"<p>Remove a client's queue (called when the client disconnects).</p> <p>Parameters:</p> Name Type Description Default <code>q</code> <code>Queue[SSEEvent | None]</code> <p>The queue returned by :meth:<code>subscribe</code> when this client connected.</p> required"},{"location":"reference/server/#nibble.server.Broadcaster.broadcast","title":"<code>broadcast(events)</code>  <code>async</code>","text":"<p>Push events to all subscriber queues and update the current snapshot.</p> <p>Parameters:</p> Name Type Description Default <code>events</code> <code>list[SSEEvent]</code> <p>The <code>SSEEvent</code> objects to enqueue for every connected client. <code>\"reset\"</code> and <code>\"update\"</code> events update the internal vehicle snapshot; <code>\"remove\"</code> events remove vehicles from it.</p> required"},{"location":"reference/server/#nibble.server.Broadcaster.current_reset_event","title":"<code>current_reset_event()</code>","text":"<p>Build a reset SSEEvent from the current vehicle snapshot for new subscribers.</p> <p>Returns:</p> Type Description <code>SSEEvent</code> <p>An <code>SSEEvent</code> with <code>event_type=\"reset\"</code> containing all currently</p> <code>SSEEvent</code> <p>tracked vehicles. Sent immediately to each new client on connection.</p>"},{"location":"reference/server/#nibble.server.configure_logging","title":"<code>configure_logging(config)</code>","text":"<p>Configure the root logger based on application settings.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>Settings</code> <p>Application settings providing <code>log_level</code> and <code>log_json</code>.</p> required"},{"location":"reference/server/#nibble.server.create_app","title":"<code>create_app(config, broadcaster, overrides, gtfs_holder, adapter=None, on_snapshot=None, initial_gtfs_fingerprint=None)</code>","text":"<p>Build and return the FastAPI ASGI application.</p> <p>When adapter is provided, the poll loop and optional GTFS reload loop are started as background tasks during the FastAPI lifespan and cancelled cleanly on shutdown.  When adapter is <code>None</code> (e.g. for OpenAPI schema generation), no background tasks are started.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>Settings</code> <p>Application settings.</p> required <code>broadcaster</code> <code>Broadcaster</code> <p>The pub/sub hub that SSE clients subscribe to and the poll loop broadcasts into.</p> required <code>overrides</code> <code>OverrideStore</code> <p>Store for operator-issued manual trip assignment corrections.</p> required <code>gtfs_holder</code> <code>GtfsHolder</code> <p>Shared container for the current static GTFS indexes, used to validate trip IDs submitted via the corrections API.</p> required <code>adapter</code> <code>BaseAdapter | None</code> <p>Feed adapter to use. When supplied, the poll loop is started as part of the application lifespan.</p> <code>None</code> <code>on_snapshot</code> <code>Callable[[dict[str, VehicleEvent]], Awaitable[None]] | None</code> <p>Optional async callback invoked after each successful poll.</p> <code>None</code> <p>Returns:</p> Type Description <code>FastAPI</code> <p>A configured <code>FastAPI</code> application with the following routes:</p> <code>FastAPI</code> <ul> <li><code>GET /vehicles</code> - SSE stream of vehicle events</li> </ul> <code>FastAPI</code> <ul> <li><code>GET /health</code> - JSON health check</li> </ul> <code>FastAPI</code> <ul> <li><code>POST /trip_assignments</code> - create a manual trip assignment</li> </ul> <code>FastAPI</code> <ul> <li><code>GET /trip_assignments</code> - list active manual trip assignments</li> </ul> <code>FastAPI</code> <ul> <li><code>DELETE /trip_assignments/{vehicle_id}</code> - remove a manual assignment</li> </ul> <code>FastAPI</code> <ul> <li><code>GET /trips/{trip_id}/predictions</code> - arrival predictions for a trip</li> </ul> <code>FastAPI</code> <ul> <li><code>GET /routes/{route_id}/headways</code> - headway metrics for a route</li> </ul>"},{"location":"reference/server/#nibble.server.gtfs_reload_loop","title":"<code>gtfs_reload_loop(config, holder, initial_fingerprint=None)</code>  <code>async</code>","text":"<p>Periodically re-download the static GTFS and reload if the feed has changed.</p> <p>Uses <code>feed_start_date</code> from <code>feed_info.txt</code> as the change fingerprint when available, falling back to an MD5 hash of the ZIP for feeds that omit <code>feed_info.txt</code>. If the fingerprint differs from the last-seen value, the fixed ZIP is published to S3 (when <code>gtfs_static_fix</code> is enabled) and the holder's <code>gtfs</code> reference is swapped to the new indexes.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>Settings</code> <p>Application settings. <code>gtfs_reload_interval_hours</code> controls the sleep interval between checks.</p> required <code>holder</code> <code>GtfsHolder</code> <p>Shared container whose <code>gtfs</code> attribute is replaced on reload.</p> required <code>initial_fingerprint</code> <code>str | None</code> <p>Fingerprint of the GTFS loaded at startup.  When provided, the first reload cycle will not trigger an unnecessary re-parse of an unchanged feed.</p> <code>None</code>"},{"location":"reference/server/#nibble.server.print_openapi","title":"<code>print_openapi()</code>","text":"<p>Print the OpenAPI schema to stdout as JSON.</p> <p>Constructs the app with stub dependencies so no live GTFS or network access is required. Pipe to a file to save: <code>nibble-openapi &gt; openapi.json</code></p>"},{"location":"reference/server/#nibble.server.main","title":"<code>main()</code>","text":"<p>Entry point: load config, download static GTFS, start server and poll loop.</p>"},{"location":"reference/state/","title":"State","text":"<p>Vehicle state machine that maintains per-vehicle confidence across polling gaps.</p>"},{"location":"reference/state/#nibble.state","title":"<code>nibble.state</code>","text":"<p>Vehicle state machine maintaining per-vehicle confidence across polling gaps.</p>"},{"location":"reference/state/#nibble.state.VehicleState","title":"<code>VehicleState</code>  <code>dataclass</code>","text":"<p>Persisted record of a vehicle's last known good state between polls.</p> <p>Holds the most recent valid trip/route/stop values so they can be carried forward when a subsequent poll arrives without a trip_id.</p>"},{"location":"reference/state/#nibble.state.StateStore","title":"<code>StateStore</code>","text":"<p>In-memory store that applies the resolution ladder to each incoming vehicle event.</p> <p>The resolution ladder (evaluated in order) determines confidence and carries forward trip information when a feed temporarily drops trip_id:</p> <ol> <li>Manual override present                 \u2192 confirmed, provenance=\"manual\"</li> <li>trip_id present + found in static GTFS  \u2192 confirmed</li> <li>trip_id present + NOT in static GTFS    \u2192 confirmed (feed takes precedence), log warning</li> <li>trip_id missing, within stale threshold \u2192 inferred (carry forward last valid trip info)</li> <li>trip_id missing, beyond threshold       \u2192 stale (reconciler emits remove)</li> <li>Never seen + no trip_id                 \u2192 stale immediately</li> </ol>"},{"location":"reference/state/#nibble.state.StateStore.get","title":"<code>get(vehicle_id)</code>","text":"<p>Return the stored state for a vehicle, or None if not yet seen.</p> <p>Parameters:</p> Name Type Description Default <code>vehicle_id</code> <code>str</code> <p>The vehicle identifier to look up.</p> required <p>Returns:</p> Type Description <code>VehicleState | None</code> <p>The stored <code>VehicleState</code>, or <code>None</code> if the vehicle has not</p> <code>VehicleState | None</code> <p>appeared in any poll yet.</p>"},{"location":"reference/state/#nibble.state.StateStore.all","title":"<code>all()</code>","text":"<p>Return a snapshot of all stored vehicle states.</p> <p>Returns:</p> Type Description <code>dict[str, VehicleState]</code> <p>A shallow copy of the internal store mapping vehicle IDs to their</p> <code>dict[str, VehicleState]</code> <p>last-known <code>VehicleState</code>.</p>"},{"location":"reference/state/#nibble.state.StateStore.update_from_event","title":"<code>update_from_event(event, gtfs, stale_threshold_seconds)</code>","text":"<p>Apply the resolution ladder and return the event with updated confidence/provenance.</p> <p>Resolution ladder (evaluated in order):</p> <ol> <li>Manual override present \u2192 <code>confirmed</code>, <code>provenance=\"manual\"</code></li> <li>trip_id present + found in static GTFS \u2192 <code>confirmed</code></li> <li>trip_id present + not in static GTFS \u2192 <code>confirmed</code> (feed takes precedence), log warning</li> <li>trip_id missing, last seen within stale threshold \u2192 carry forward, <code>inferred</code></li> <li>trip_id missing, beyond stale threshold \u2192 <code>stale</code> (caller emits remove)</li> </ol> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>VehicleEvent</code> <p>The raw <code>VehicleEvent</code> from the current poll.</p> required <code>gtfs</code> <code>StaticGTFS</code> <p>Static GTFS indexes used to validate <code>trip_id</code> lookups.</p> required <code>stale_threshold_seconds</code> <code>int</code> <p>Maximum seconds since last valid observation before a vehicle is considered stale.</p> required <p>Returns:</p> Type Description <code>VehicleEvent</code> <p>A new <code>VehicleEvent</code> with <code>confidence</code> and <code>provenance</code> set</p> <code>VehicleEvent</code> <p>according to the resolution ladder. The internal store is updated</p> <code>VehicleEvent</code> <p>as a side effect.</p>"},{"location":"reference/state/#nibble.state.StateStore.remove","title":"<code>remove(vehicle_id)</code>","text":"<p>Remove a vehicle from the store (called when it disappears from the feed).</p> <p>Parameters:</p> Name Type Description Default <code>vehicle_id</code> <code>str</code> <p>The vehicle identifier to remove. No-op if not present.</p> required"}]}